---
status: archived
last-reviewed: 2026-06-28
review-trigger: "Update as items are walked, edit as scope adjusts"
---

# The Shelter's Side — Walkthrough

Verification checklist for the Shelter's Side phase. **Concise by design** — surface only what's worth the reviewer's judgment, what risks regression, and what confirms the phase thesis.

**Phase thesis:** the shelter's *own* (operator) view is now real enough to **show and to interview against** — chiefly the **handover** (check-out → back-safe check-in, logged) — with the application landing, walker pool, and adoption-interest landing at illustrative parity, the shelter walkthrough's back half *demonstrating* it (POV switches to the operator), and a feasibility kit driving the interviews.

**How to use:**

1. Dev server on port 3000.
2. Enter the operator POV: the demo switcher → **Útulek Liběň (Shelter operator)**, or the `?admin=1` escape hatch on any shelter.
3. Walk top-to-bottom.

**The back-office shell (revised 2026-06-24, PO co-design; IA refined 2026-06-28).** Entering operator mode swaps the whole app shell for the shelter's back-office: the nav becomes **Útulek Liběň (hub) · Schedule · Applications · Inbox · Notifications · Profile** (Community + Discover drop). **Schedule** (replaces My Schedule) = today's handover board + upcoming walks. **Applications** (replaces Bookings) = the inbound queue across the commitment ladder, tabbed **Walks / Stays / Adoptions** (URL-addressable via `?tab=`). **Inbox** = an honest "shelter messaging is coming" stub. The shelter **hub** (`/shelters/[id]`) holds **Feed / Dogs / Walkers** (Adoptions moved to Applications 2026-06-28); neither operator nor public-preview view carries a top banner — "View public page" and "Back to operator view" both live in the Feed hero (matching hero-button pattern). Vouched progression (tier pill / mentee progress) now reads on every walker row.

**Seed context (today at Útulek):** the handover board (Schedule → Today) is seeded with a realistic morning — Maja (due to collect, Pavel D.), Tonda (out walking, Marie B.), Líza (walk finished, awaiting back-safe, Lukáš P.), Theo (back safe, Anna K.), and a group-walk batch (Edda + Nora, led by Klára) — plus upcoming walks over the next few days (relative-dated, so the Upcoming day headers shift with today). Applications → **Walks**: 3 pending (Petra S. invited, Radek N. + Jan D. new), each with vetting-signal chips (availability, "fills your … gap," mentor-recommended). Applications → **Stays** (illustrative): Marie K. sleepover + Tomáš R. adoption trial. Applications → **Adoptions**: Nora + Maja + Theo interested, Káťa pending. Times render in your local timezone (seed times are UTC).

---

## Open for your call

Identifier prefix: **`O`**.

- [x] **O1. Persona roster trim — dropped Tomáš, Lena, Magda from the *picker*.** The free-roam picker now carries Tereza, Daniel, Klára, Eliška, **Útulek Liběň (operator)**, New User. Tomáš/Lena/Magda stay full mock-world users — still reachable via `?as=<id>` and still appearing as characters in content (Magda is the neighbour in Daniel's new-owner story) — they're just off the free-roam menu. No walkthrough switches POV into a dropped persona (the arcs run as Daniel / Klára / Eliška / operator). Ratify the cut list, or keep/cut others. (See any picker: `/profile` name dropdown, or `/`.)
- [x] **O2. Group multi-dog release is shown as a PROPOSAL, not a committed model.** The batch card proposes "the mentor signs out the group as the responsible walker — one check-out for the batch." This is the open FC18 checkout question, deliberately framed as a question to shelters, not an asserted flow. (Operator → Today → the "Stromovka morning walk" batch card.)
- [x] **O3. The operator is modeled as a synthetic `UserProfile` persona (`op-utulek-liben`), not a new entity layer.** A shelter isn't a UserProfile, but modeling the operator AS a minimal persona (shelter logo as avatar) lets it flow through the existing switcher/`useCurrentUser` machinery with minimal plumbing. Tradeoff: on non-shelter surfaces it renders as a sparse locked account. Acceptable for a demo affordance? (Switch to it, then visit `/home`.)
- [x] **O4. Beats 3–4 of the shelter walkthrough switch POV to the operator persona.** The demo viewer literally "becomes the shelter" mid-walkthrough to see their own side. A strong move; the alternative was staying Eliška and just navigating to the operator view. (Run the `shelter` walkthrough → Beat 3.)
- [x] **O5. Seeded illustrative content choices.** The "today at Útulek" handover seed, 3 pending applications (directory-style applicants with denormalized names + vetting-signal chips), 3 seeded adoption interests (Nora/Maja/Theo), and 2 illustrative **Stays** requests (Marie K. sleepover + Tomáš R. adoption trial) are all mock-data calls to make the operator surfaces read populated. Seeding Nora interested means her dog page shows the "interested" adopt state (Walk still works). Right amount of seeded depth?
- [x] **O5b. "Stays" direction (2026-06-28).** Built illustrative (no data model): one queue, two flavours tagged by intent — **Sleepover** (respite/exposure, leaning into the advocacy loop) and **Adoption trial** (foster-to-adopt). Recommendation was the broad "dog goes home for a while" framing so interviews can tell us which flavour matters before we build for real. Confirm the framing, or narrow it (respite-only / trial-only).
- [x] **O6. The operator gets a full back-office shell** (PO co-design 2026-06-24, ratified live). Entering operator mode swaps the whole app nav (Útulek/Schedule/Applications/Inbox/Notifications/Profile), branches `/schedule` → walks, `/bookings` → Applications, `/inbox` → stub, and reshapes the hub to Feed/Dogs/Walkers. **Sub-call resolved 2026-06-28:** Adoptions left the hub and joined **Applications** as a tab (with Walks + Stays), so the hub holds Feed/Dogs/Walkers and Applications is the inbound-ladder queue. Walkers stays a hub tab (the roster), not promoted to nav.
- [x] **O7. Added `text-volunteer` / `bg-volunteer-*` Tailwind utilities** (`@theme` mappings of the existing `--status-volunteer-*` family) — parity with `info`/`brand`, used across the new operator surfaces. Design-system addition. (`docs/implementation/design-tokens.md` will note it at close.)

---

## Worth verifying

Identifier prefix: **`V`**. One check per item.

### V1 — Operator entry + back-office shell (WS-A)

- [x] **V1.1 Operator persona swaps the whole app shell.** Demo switcher → **Útulek Liběň (Shelter operator)** → *Expect:* lands on the shelter hub; the left nav is now **Útulek Liběň · Schedule · Applications · Inbox · Notifications · Profile** (no Community / Discover). *(Verified 2026-06-28.)*
- [x] **V1.2 `?admin=1` escape hatch still works.** `/shelters/utulek-liben?admin=1` as any persona → *Expect:* the operator hub renders.
- [x] **V1.3 An operator persona on a *different* shelter sees the public page.** As the operator persona, open `/shelters/pes-v-nouzi` → *Expect:* the normal public page (you only operate your own shelter).
- [x] **V1.4 Public ↔ operator round-trip.** Hub → "View public page" (in the Feed hero, no banner) → *Expect:* public page whose Feed hero carries a "Back to operator view" button (also no banner) → tap it → back to the hub (Feed/Dogs/Walkers). Both directions use the same hero-button pattern. *(Verified 2026-06-28.)*
- [x] **V1.5 A dropped persona is still reachable.** Open `/profile?as=magda` → *Expect:* Magda's profile loads (dropped from the picker, still switchable via the `getUserById` fallback; she also still appears as a character in the new-owner story). *(Verified 2026-06-28. Corrected from an earlier wording that claimed the new-owner walkthrough switches POV into Magda — it doesn't; it runs as Daniel.)*
- [x] **V1.6 Consumer shell is unbroken.** Switch back to Tereza → *Expect:* the normal nav returns (Community / Discover / My Schedule / Bookings / Inbox / Notifications / Profile); `/schedule` + `/bookings` + `/inbox` render their consumer surfaces.

<!-- (picker-roster check folded into O1 — same thing) -->

### V2 — Schedule: handover board (Today) + Upcoming + History (WS-B)

*Operator **Schedule** is three tabs: **Today** (the handover board, V2.1–V2.4, V2.6), **Upcoming** (V2.5), **History** (V2.7).*

- [x] **V2.1 Check out a due dog.** Operator → **Schedule** → Today → Maja's row → **Check out** (in the row's footer) → *Expect:* Maja moves from "Due to collect" to "Out now," and the footer now offers **Undo check-out** (revert to due). *(Verified 2026-06-28 — per-row undo added this pass.)*
- [x] **V2.2 Confirm back safe.** Lukáš P. · Líza (status "Walk done …") → **Back safe** → *Expect:* moves to "Back safe today" (the status becomes the dog's green label); the "back safe" count ticks up; the footer now offers **Undo** (revert to out). *(Verified 2026-06-28 — per-row undo added this pass.)*
- [x] **V2.3 Group batch release.** The "Stromovka morning walk" card → **Check out all** → *Expect:* both Edda + Nora flip to released (each now offers a "Back safe" control + an inline **Undo check-out**, so a wrong release is reversible per dog).
- [x] **V2.4 Live walker status reads through.** In **Today → Out now**, the dog's status label distinguishes a walk in progress (Tonda · "Out since 04:35 PM") from a finished-but-not-returned walk (Líza · "Walk done 04:20 PM").
- [x] **V2.5 Upcoming tab.** Open Schedule → **Upcoming** → *Expect:* future walks grouped by day (the seed uses relative dates, so the day headers shift with today), read-only (no check-out actions — that's Today's job), with a "Jump to a date" picker to look further ahead.
- [x] **V2.6 Vouched progression on rows.** *Expect:* every walker row (Schedule + Walkers) carries a tier pill — Pavel D. "Super Volunteer," others "Volunteer."
- [x] **V2.7 History tab.** Open Schedule → **History** → *Expect:* a concise read-only record of completed (back-safe) walks, with a note that fuller reporting/exports come later.

### V3 — Existing surfaces, illustrative parity (WS-C)

- [x] **V3.1 Application advance round-trip.** Operator → Applications → **Walks** tab → Radek N. **Invite to visit** (applied→invited), then **Vouch as walker** (invited→vouched) → *Expect:* Radek leaves the queue and appears on the shelter hub's **Walkers** tab. *(Verified 2026-06-28 — invite/vouch + the new Undo both round-trip.)*
- [x] **V3.2 Adoption advance.** Operator → Applications → **Adoptions** tab → Maja **Arrange meet-and-greet** (interested→pending), then **Finalise adoption** → *Expect:* stage advances; finalised dog leaves the active list. *(Verified 2026-06-28 in its new Applications home.)*
- [x] **V3.3 Walker pool controls.** Operator → Walkers → a walker's **⋯** → Promote/Demote → *Expect:* the tier badge updates; demoting a Super Volunteer's only `trusted` standing pulls the top tier.

### V4 — Shelter walkthrough back half (WS-D)

*One end-to-end run of the **shelter** walkthrough covers all three.* (URLs repaired + verified 2026-06-28; the guided drive itself still needs a walk.)

- [x] **V4.1 POV switch.** Beat 3 → *Expect:* handoff reads "You're now Útulek Liběň · Shelter operator," lands on the operator handover board.
- [x] **V4.2 Drives the back-office surfaces.** *Expect:* `/schedule` (handover) → `/bookings` (Applications · Walks) → `?op=walkers`; Beat 4 → `/bookings?tab=adoptions` → hub Feed.
- [x] **V4.3 Soft close.** *Expect:* "If you run a shelter, this part's for you." + three light questions (no per-beat probes).

---

## Decisions surfaced during walkthrough

<!-- Design-system note (surfaced during this phase's review, applies app-wide) -->

- **Dog square avatars now use a single `--radius-dog` token (20%, a PERCENTAGE) → `rounded-dog` utility, swept app-wide.** A fixed px radius read inconsistently by size (~40% round on a 20px thumb, ~6% on the 200px hero); a percentage scales the curve so dog avatars look identically rounded everywhere. 20% = the value the dog-profile hero already used. Applied to every SQUARE dog avatar (hero, pet-card avatar/gallery thumbs, sessions pet header compact, person/sched dog avatars, attendee-stack dogs, shelter sheets, the handover/schedule/modal thumbs). **Excluded** rectangular dog *card images* (`.shelter-dog-card-photo`, SessionsPetHeader full variant) — a % radius gives a non-square element elliptical corners; those keep `rounded-panel`. Token + styleguide entry added. → `docs/implementation/design-tokens.md` + `design-system.md` (Avatar Rule B)


A running log. Each entry carries a `→ target-doc` for the phase-close sweep.

- **Operator persona = synthetic `UserProfile` (`op-utulek-liben`) + `getOperatorShelterId` helper; picker routes operator picks to `/shelters/<id>`.** Shelter modeled as a minimal persona so it rides the existing switcher. → `features/demo-mode.md` (+ `features/shelters.md` operator-view entry)
- **Persona roster trimmed (Tomáš/Lena/Magda off the picker); switchability decoupled from picker membership via `setUserById` → `getUserById` fallback.** → `features/demo-mode.md`
- **Handover data model: `BookingSession.releasedAt` / `returnedAt` (operator check-out / back-safe check-in), distinct from the walker's `checkedInAt` / `report.completedAt`.** → `features/shelters.md`
- **Operator handover board ("today's walks") — the hero surface; reads shelter-walk `Booking`s, writes the custody trail.** Seeded "today at Útulek" in `mockBookings.ts` (BOOKINGS_SEED_VERSION→2). → `features/shelters.md`
- **Group multi-dog release shown as a proposal (mentor-as-responsible-party), not committed.** → `features/shelters.md` + Open Q FC18-tension-2 (advance/keep-open at close)
- **Operator gets a full back-office app shell (PO co-design 2026-06-24).** Operator mode branches the nav (`Sidebar`/`BottomNav` → Útulek/Schedule/Applications/Inbox/Notifications/Profile via `useOperatorShelterId`) and the consumer pages: `/schedule` → shelter walk schedule (today's handover board + upcoming), `/bookings` → the walker Application queue, `/inbox` → an honest "shelter messaging is coming" stub. The shelter hub (`/shelters/[id]`) reshapes to Feed/Dogs/Walkers/Adoptions; sections URL-addressable via `?op=`; `?admin=1` still enters the shell. → `features/shelters.md` + `features/demo-mode.md`
- **Handover board date-aware (today vs upcoming); vouched progression (`WalkerTierPill`) surfaced on every walker row** (Schedule, handover, walker pool). Seeded upcoming walks (BOOKINGS_SEED_VERSION→3). → `features/shelters.md`
- **Shelter messaging confirmed as a real future need** (operator Inbox kept as a stub, not hidden) — institutional `Conversation` party model widening is FC16 / Open Q §14. → no new doc (already logged in §14/FC16)
- **The group-walk "Proposal: …" question removed from the UI** (PO review 2026-06-24). It read as an interview question sitting in product chrome; the proposal/checkout question lives only in the feasibility kit + docs now, not on the handover card. The batch release still works; it's just no longer captioned as a question. → `features/shelters.md` (note the in-UI proposal text is gone) + Open Q FC18-tension-2
- **Operator action buttons use `ButtonAction` (form radius), not pill-styled `<button>`s** (PO review 2026-06-24) — so actions read distinctly from the round credential/status pills (which were confusingly identical). Primary = `volunteer`, secondary/confirm/decline = `outline`. Colour restraint pass: summary numbers neutral (not per-state coloured), group card de-tinted (was washed-out violet insets). → `features/shelters.md` + `design-system.md`
- **Interstitial secondary (Back) button given pill radius + subtle hover fill** to match the primary CTA (was: no radius → partial corners on hover). → `design-system.md` / `landing-page` (walkthrough chrome)
- **Operator Schedule got tabs (Today / Upcoming / History), mirroring the consumer Schedule** (PO 2026-06-24). Today = the handover board (larger cards, actions, empty-state when nothing's on). Upcoming = future walks, smaller cards, no actions, day-grouped + a "Jump to" date picker. History = a concise back-safe record (seeded past walks; `BOOKINGS_SEED_VERSION→4`), with a note that fuller reporting/exports come later. → `features/shelters.md`
- **Group-walk handover card refinements** (PO 2026-06-24): green circle avatar marks it as a community meet (sized to the dog rows); walker avatars added to the group rows (matching solo cards); the walk title is the link to the meet (dropped the separate "View walk" button); **individual check-out per group dog** added alongside "Check out all" (so a no-show doesn't block the rest). → `features/shelters.md`
- **Walker is the protagonist on the shelter's side (PO review 2026-06-24).** The handover/schedule rows lead with the WALKER (56px circle avatar + name + tier), with the dog demoted to a small secondary thumbnail + name on a second line (the shelter knows its dogs; it needs to identify and clear the person). This intentionally inverts the pet-as-protagonist convention, which is correct for *owner* care surfaces but not for a custody/hand-off surface. → `features/shelters.md` + (note the exception to pet-as-protagonist) `design-system.md`
- **Appointment time surfaced (PO 2026-06-24).** Added `BookingSession.startTime` (HH:MM); shelter walks are seeded with collection times (`BOOKINGS_SEED_VERSION→5`). Rows show the scheduled time on due/upcoming ("Maja · 11:00 AM") and live status on active ("out since 4:35"). The time/status moved off the cramped under-button line into the dog's secondary line.
- **Walker hand-off modal — FIRST CUT (PO 2026-06-24, build-now agreed; contents still being refined).** Clicking a walker opens `WalkerHandoverModal`: identity (avatar/name/tier/vouched-since), what they're here for (dog + booked time), a **Clearance** checklist (identity verified, platform + shelter waivers with signed dates, eligible-for-this-dog via tier vs the dog's policy), a **Record** line (walks here, last walked), and a profile link when the walker bridges to a UserProfile. **Open / to refine with PO:** waiver dates are derived from `vouchedAt` for seeded roster walkers (no real e-sign data until FC16) — clearly representative; decide what else the shelter needs at hand-off (incident history? per-dog notes? the actual forms?). → `features/shelters.md` (new operator surface) + `design-system.md`
- **Operator surface visual conventions settled (PO review 2026-06-24).** Action buttons use the **`secondary`** `ButtonAction` variant (NOT `neutral` — `neutral` overlaps `secondary` and shouldn't have been used; it stays in the codebase for now since communities/meets use it, flag for a future consolidation). Cards use **`surface-top`**. Handover/group rows are a single shared `HandoverRow` (a `bare` variant for the group card) so every size matches (56px dog avatars, text-base names, walker avatars). The group meet avatar is **`brand-subtle` + `brand-light` border**, 56px. Status/time label moved OFF a third text line to under the action (due rows show none; out rows show "Out since / Walk done"; back rows show the confirmed marker). On mobile the action wraps to its own full-width row (was crowding the content). Mobile header relabeled per operator slot (Schedule / Applications). → `design-system.md` + `features/shelters.md`
- **Application queue + adoption-interest landing built as operator panels; seeded 3 pending applications (`WalkerApplication.applicantName`/`applicantAvatarUrl` denormalized fields, APPLICATIONS_SEED_VERSION→2) + 3 seeded adoption interests (ADOPTION_SEED_VERSION).** Adoption-interest landing is the one genuinely-new operator surface (the others verified existing). → `features/shelters.md`
- **`text-volunteer` / `bg-volunteer-*` / `border-volunteer-*` Tailwind utilities added to `@theme`** (mappings of `--status-volunteer-*`). → `docs/implementation/design-tokens.md` + `design-system.md`
- **Shelter walkthrough back half (Beats 3–4) now switches POV to the operator persona and demonstrates the real surfaces** (was "described, not shown"); vouch fires at end of Beat 2; soft-close copy settled (three questions). → `strategy/Demo Narrative.md` + `features/demo-mode.md`
- **Feasibility interview kit authored** (`strategy/Shelter Feasibility Interview Kit.md`) — pain-point ladder + 9-item pain-point map + per-surface prompts. **Open Q §14 binding-constraint reframed to the ladder.** → already landed (graduate/confirm at close)

<!-- 2026-06-28 review pass (PO live) -->

- **Applications reframed into an inbound-ladder queue, tabbed Walks / Stays / Adoptions (2026-06-28).** Adoptions moved off the shelter hub into Applications (joining the new Stays tab); the hub is now Feed/Dogs/Walkers. Tabs are URL-addressable via `?tab=` (mirrors consumer Bookings `?tab=` and the hub `?op=`) so they deep-link and the guided walkthrough can drive them. → `features/shelters.md` (hub chrome: Adoptions tab removed; Applications IA)
- **"Stays" tab added — illustrative (2026-06-28).** New `ShelterStaysPanel`: one queue, two flavours tagged by intent (Sleepover = respite/exposure; Adoption trial = foster-to-adopt). No data model yet (deliberate — pending interview signal); seeded 2 representative requests, "Plan the stay" is an honest stub. Sibling of the Adoptions/Applications panels (shared card language). → `features/shelters.md` (+ revisit flavour post-interview, see O5b)
- **Operator banners removed both directions; "View public page" + "Back to operator view" moved into the Feed hero (2026-06-28, PO call).** Nothing sits above the tabs either way; the back-office shell already signals operator context. `OperatorBanner` AND `OperatorReturnStrip` deleted — the public↔operator round-trip now uses the matching hero-button pattern on both sides (operator hub Feed hero → "View public page"; public-preview Feed hero → "Back to operator view," gated on `?public=1` via a `showOperatorReturn` prop on the shared `FeedTab`). Home-nav (Útulek) remains the always-available fallback out of any public non-Feed tab. → `features/shelters.md`
- **Application queue reworked into a decision queue (2026-06-28).** Grouped **New — needs a reply** / **Invited — awaiting their intro visit**; compact triage rows with scannable vetting-signal chips (availability, "fills your … gap" matches-need, mentor-recommended); one inline primary action (Invite / Vouch). New `ApplicantDetailModal` (sibling of `WalkerHandoverModal`) holds the full picture + actions. **Coordinator note is real-ish** (`WalkerApplication.coordinatorNote` + `setNote`, persisted locally) — demonstrates the capability; team-shared notes are FC16. **Message stub removed** — messaging is the profile link (chat lives on profiles). **Undo bar** (`revertState`) protects an accidental Invite/Vouch (earns its keep on Vouch, which otherwise leaves the queue silently). New fields on `WalkerApplication`: `availability` / `experience` / `nearby` / `matchesNeed` / `coordinatorNote` (illustrative, seeded). → `features/shelters.md` + `design-system.md`
- **Quote/note text blocks aligned to the bordered-box convention (2026-06-28, PO call).** The applicant message + stay-note quotes moved from `surface-inset` + `rounded-md` to **`surface-base` + full `border-edge-regular` + `rounded-sm`** (matches `.inquiry-card-notes` / the common bordered-box; the left-accent treatment stays reserved for schedule/status cards). **Consolidation target flagged:** "quoted note" now exists in ~5 variants (`.inquiry-card-notes`, `.proposal-form-notes`, `FeedCareReview`, `BookingProposalCard` override, these) — a shared quote-block is a future cleanup. → `design-system.md` (consolidation target)
- **Shelter walkthrough Beat 4 repointed for the new IA (2026-06-28).** `startUrl` → `/bookings?tab=adoptions` (was the dead `?op=adoptions`); copy ties the inbox together ("Applications gathers everyone reaching toward your dogs — walks, stays, adoptions"); final step's `advanceOn` relaxed to the bare hub path so the home-nav hop completes it. Stays deliberately gets no dedicated beat (illustrative). → `strategy/Demo Narrative.md` / `walkthroughBeats.ts`
- **Undo on the operator surfaces — per-row on the handover board, top-alert on Applications; neutral colour (2026-06-28, PO walk).** Handover check-out / back-safe carry a **per-row Undo** (revert to the prior custody state) rather than a top alert — the alert is last-action-only, so an earlier mistake during a busy check-out run was unrecoverable; per-row keeps every actioned row reversible at any time. Solo handover cards moved to a **footer action row** (content on top, actions in a bottom row separated by a top border + `surface-base` fill — mirroring the schedule review cards), so undo + the primary action share one row at one height (and it reads identically on mobile). Group-walk inner rows stay lighter inline (the group card supplies the dividers). The application queue keeps the top **`UndoBar`** (extracted to `components/ui/UndoBar.tsx`, token re-arm + auto-dismiss) because **vouch removes the row**, leaving nothing to attach an inline undo to. Undo affordances are **neutral** (`fg-primary` in the bar / `fg-tertiary` inline), NOT volunteer violet — undo is a utility action, not a volunteer-path one. → `features/shelters.md` + `design-system.md` (handover footer-action pattern; UndoBar; undo = neutral)
- **Group-walk card: dropped the vestigial bottom divider (2026-06-28, PO walk).** "Check out all" moved to the header earlier, so the dog-row group needs only a top divider (`border-t`), not `border-y` — no trailing border under the last row. → `features/shelters.md`
- **Stale "walkthrough switches to Magda" claim corrected (2026-06-28, PO walk).** No walkthrough switches POV into a dropped persona; new-owner runs as Daniel (Magda is a character in his story). Fixed the wrong claim in `personas.ts` (two comment blocks) + the walkthrough doc; V1.5 re-pointed to the real `?as=magda` reachability test. → `features/demo-mode.md` (persona switchability ≠ picker membership)
