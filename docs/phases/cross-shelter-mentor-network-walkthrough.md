---
status: active
last-reviewed: 2026-06-10
review-trigger: "Update as items are walked, edit as scope adjusts"
---

# Cross-Shelter Mentor Network — Walkthrough

Verification checklist for the Cross-Shelter Mentor Network phase. **Concise by design** — the build was driven end-to-end in the preview during development (mentee loop, graduation, solo walk booking, portability, non-accepting fallback, credit toggle all exercised); this walkthrough surfaces what's worth YOUR judgment.

**How to use:**

1. Run the dev server (`npm run dev`, port 3000).
2. Switch personas via the profile-page name dropdown, the `/` launcher, or the `?as=<personaId>` URL param.
3. **Most items want a fresh demo state** — clear localStorage (or use a private window) before V1; later V items build on V1's state deliberately.

**Status legend:** `[ ]` not yet walked · `[x]` walked, no issues.

**Key state for this phase:** Útulek accepts mentor-vouches (min 3) · Pes v nouzi accepts (min 5) · Druhá šance does NOT accept. Klára is seeded as Útulek's second trusted walker (64 walks · 60 shelter-credited) and the only mentor (450 Kč/session at Útulek + Pes v nouzi).

---

## Open for your call

Decisions made during the build instead of stopping to ask — ratify or redirect (CONTRIBUTING.md → decide-and-flag).

- [x] **O1. Mentor booking skips the inquiry → proposal → sign round-trip.** Resolved 2026-06-10 — direct booking ratified (fixed price, no sign step), with two additions: a **booking-confirmation card** in the chat (appointment-confirmation framing, links to the booking like the other artifacts) and **mutual Connected on the first completed session** (the flow's Connected moment, replacing the contract-sign trigger it doesn't have). See Decisions surfaced #1 + #2. (Tomáš → `/profile/klara?tab=services` → Book a session on "Mentored shelter walk".)
- [x] **O2. Tomáš is the storyline mentee.** Pre-approved direction ("reuse a persona, not Daniel") — ratify the specific pick. He's the A8 latent-demand thesis in person (9–6 professional whom Útulek's weekday filter excludes); his thread doesn't touch the V2 Daniel→Klára narrative. (Drive V1 as Tomáš.)
- [x] **O3. Platform Super Volunteer renders as a status pill on the Volunteer-work section title row** + "Recognized at every participating shelter" subtitle — no walk-count totals (those were dropped 2026-06-09; the tier is a status, not a stat). This is the reconciliation flagged at phase open (conflict 5). (Tomáš → `/profile/klara` → Volunteer work.)
- [x] **O4. Demo platform-tier rule: cross-shelter walks ≥ 25 AND ≥ 1 trusted affiliation.** Resolved 2026-06-10 — the rule stands, with the gap it exposed closed: tier escalation no longer auto-promotes unchecked. Walk-count thresholds are now SUGGESTIONS; the shelter promotes/demotes freely via per-row tier overrides (Members tab "(demo)" dropdown), and because platform Super Volunteer requires a trusted affiliation, the shelter's lever transitively controls platform status + mentor eligibility. See Decisions surfaced #4. (Verify via V9.)
- [ ] **O5. Klára's bootstrap is seeded statically** (Útulek roster: 64 walks, 60 credited) rather than driven live, so the world is coherent from any entry point — her mentor offering must be live before the demo's first beat. The crediting MECHANISM stays drivable live via the toggle (V4). (Útulek → Members → Klára H. row.)
- [ ] **O6. Crediting now lives in TWO places** (reshaped 2026-06-10 with the Members-row kebab). The row kebab carries "Credit historical walks (demo)" targeting THAT walker — the operator-honest framing that resolves the original "why can the walker credit themselves" weirdness. The walker-button self-credit REMAINS for one storyline: bootstrapping the current persona at a thin shelter, where they have no Members row yet. Ratify keeping both, or cut the self-credit and accept that thin-shelter bootstrap demos via a different persona's row. (Útulek → Members → any walker row dots menu; thin shelter → walker-button dropdown.)
- [ ] **O7. Mentor service card uses neutral chip chrome** ("Shelter mentoring" + duration + shelter-name chips) — not the violet volunteer family or the blue care family. It's a PAID service (blue?) rendered next to volunteer credentials (violet?); neutral dodges the conflict. Could go either way. (`/profile/klara?tab=services`.)
- [ ] **O8. Graduation message comes from the MENTOR, not "the shelter."** Shelters can't message (no institutional conversation infra); Klára announcing her own vouch ("I've vouched for you at Útulek") is honest and warmer than faking a shelter sender. (Inbox after V1's third session.)
- [x] **O9. The shelter-facing demo drives the storyline LIVE from fresh state** rather than pre-staging Tomáš mid-mentorship (board H1 said "pre-staged"). The state-toggles make live driving fast, and a pre-staged state would collide with demonstrating the booking beat. **Ratified 2026-06-10** — live-drive stands for coordinator interviews; feature-focused GUIDED walkthroughs (with per-walkthrough pre-staged state) are future work after the feature/surface arc settles → see FC17 in `planning/Future Considerations.md`.

---

## Worth verifying

- [ ] **V1. The full mentee loop, fresh state, as Tomáš.** Clear localStorage → `/shelters/utulek-liben?as=tomas`. The "New to shelter walking?" mentor card shows Klára at 450 Kč. Book a mentored walk → sheet shows shelter picker (Útulek/Pes v nouzi), "session 1 of 3" context, BOTH waiver rows as checkboxes → tick + date + submit → success names the remaining-session count. The chat thread carries a "Booking confirmed" card whose View booking link opens the booking. Button now reads "In mentorship · 0/3"; progress line below the action row. Dropdown → Complete mentor session (demo) — after the FIRST completion Tomáš and Klára are mutually Connected (check her profile hero). ×3 total → button flips to "Vouched walker", graduation message lands in Inbox from Klára, the mentor booking in `/bookings` shows completed with a sealed report.
- [ ] **V2. Waiver sign-once carry-over.** Continuing V1's state: book a mentor session at Pes v nouzi (via Klára's profile, switch the shelter pill). The platform waiver row shows "signed {date} — carries across shelters" ✓; the Pes v nouzi waiver row is a fresh checkbox. Graduation context says 5 sessions (their override), not 3.
- [ ] **V3. The real solo walk (P77's replacement).** Continuing V1: `/dogs/shelter-dog-tonda?as=tomas` → status "You walk at Útulek Liběň" → Walk Tonda opens the WalkBookingSheet (not a dropdown) → book → `/bookings` My Services shows "Útulek Liběň · Tonda · Volunteer · no charge" → detail page: shelter logo as the partner, partner link routes to `/shelters/utulek-liben`, PET INFO resolves Tonda from the roster. Start → Finish the session and confirm the Members-tab walk count ticks up (booking-derived).
- [ ] **V4. Credit toggle.** Any persona at a thin shelter → apply (or not) → walker-button dropdown → Credit historical walks (demo) → instantly vouched; Members row and profile Volunteer-work row read the plain "25 walks" (provenance stays in the data — Decisions #6). Crediting an EXISTING walker via their Members-row kebab sums on top of their seeded count (Helena: 18 → 43).
- [ ] **V5. Druhá šance non-accepting posture.** As a mentee with sessions (post-V1 Tomáš): no mentor card on their Feed; apply → sheet carries "Mentor-recommended · 3 sessions with Klára Horáčková"; after applying, the progress line explains the shelter reviews walkers directly.
- [ ] **V6. Eligibility still composes.** As a freshly-vouched vetted walker, an `experiencedHandlersOnly` dog (Šimon, Berta) shows the explanatory state with a DISABLED Walk button; Tonda books fine. Strictest-rule-wins unchanged.
- [ ] **V7. Klára's surfaces as the mentor.** `/profile/klara`: Volunteer work shows the Super Volunteer platform pill + "64 walks · 60 credited by the shelter"; Services tab shows the mentor card with shelter chips; her OWN Services edit mode shows the read-only mentor card ("managed with the shelters you mentor at"). At Pes v nouzi her application sheet shows the Super Volunteer recognition line; she does NOT see her own mentor card anywhere.
- [ ] **V8. V2 demo narrative untouched.** Run the guided walkthrough from `/`. Klára's Services surfaces now include the mentor card (additive); confirm no beat's framing breaks and the Daniel thread plays as before.
- [ ] **V9. Shelter tier authority (O4 resolution).** Útulek Members tab: every walker row carries a quiet dots menu (flush right, opens leftward) → Demote Klára → her pill drops to "Volunteer" (no annotation — the roster just shows the shelter's call), the Feed's mentor card disappears, and her profile loses BOTH the platform Super Volunteer pill and the per-shelter Super Volunteer tier. Promote her back → everything restores. Ladder ends hide the irrelevant direction (no Promote at trusted, no Demote at vetted). Bonus coherence fix to confirm: as Klára (static-roster walker, no application record), dog pages now treat her as vouched — "Walk Šimon" opens the real booking sheet.

---

## Decisions surfaced during walkthrough

*(Append as you walk — each entry carries a `→ target-doc.md` annotation for the phase-close sweep.)*

1. **`booking_confirmation` chat artifact for direct-booked services** (O1 review, 2026-06-10). Direct booking ratified — but the chat keeps a structured booking artifact: a new `booking_confirmation` `MessageType` + `ChatMessage.bookingRef` + `BookingConfirmationCard` (reuses the `.inbox-proposal-*` accepted treatment; "Booking confirmed" header, no sign stamp, View booking → link). Appointment-confirmation framing, NOT contract framing — nothing to approve. The chronicle rule generalizes: *every booking in a relationship is findable from its chat thread*, whether it arrived via proposal-sign or direct booking. Generic shape — any future instant-book flow posts the same type. → `features/messaging.md` (new message type + card) + `strategy/Groups & Care Model.md` → Services as Catalog (mentor_session booking path).
2. **Completed mentor session → mutual Connected** (O1 review, 2026-06-10). The mentor flow has no contract-sign (the Connected trigger on other paid flows), so the first COMPLETED session marks mentor + mentee mutually Connected — supervised in-person dog handling is a stronger trust event than a signature, arguably the most on-thesis Connected transition in the app. Booking alone still only marks Familiar (consistent with inquiry-send). Implemented in `useMentorSessionCompletion` (idempotent across sessions). Alternative considered and parked: prompting both parties instead of auto-marking — revisit if user testing shows auto-Connected feels presumptuous. → `strategy/Trust & Connection Model.md` → "Inquiry-driven transitions" (add the mentor-session rule).
3. **Tomáš's persona copy carries the mentee arc** (O2 review, 2026-06-10). Landing card goal reworked: *"Boards Hugo with a sitter he trusts when work travel hits — and wishes his 9-to-6 left room to walk shelter dogs"* — the wish framing stays honest in fresh demo state (his mentee journey is drivable, not pre-staged) and sets up the mentor path as its answer. Switcher tagline gains "; drawn to shelter dogs." His demo-mode highlight reel should pick up the mentee surfaces (mentor booking, graduation, volunteer-work section) at phase close. → `features/demo-mode.md` (Tomáš highlight reel + cast description refresh).
4. **Tier thresholds are suggestions; the shelter promotes/demotes freely** (O4 review, 2026-06-10). The gap O4 exposed: tier escalation auto-promoted on walk count alone — 25 walks crowned a Super Volunteer with no shelter say, and that flowed into platform tier + mentor eligibility. Resolution (PO call): NOT an approval step (that recreates the admin queue the phase removes) — auto-promotion stays the zero-admin default, but the shelter holds free override authority both directions. Data: persisted `tierOverrides` map keyed `(shelterId, userId)` in `WalkerApplicationsContext`; effective tier = override ?? seeded/derived, threaded through every reader (affiliations, platform tier, mentor gate, Members tab, dog-page eligibility). Surface: "(demo)" dots dropdown per Members-tab walker row (operator stub per scope discipline 2; real home is FC16 walker pool management, updated). The shelter's lever reaches the whole stack: demoting a mentor's only trusted affiliation revokes platform Super Volunteer + mentor offerings (verified live). *(Initial "tier set by shelter" row marker removed same day — see #6.)* Rode along: static-roster walkers (Klára) now resolve as vouched on dog pages — they could not book walks before. → `features/shelters.md` (tier model: thresholds-as-suggestions + override authority + provenance marker) + `implementation/badges.md` (volunteer tier earning note).
5. **Members-row kebab is the per-walker operator surface** (O4 follow-up + O6 reshape, 2026-06-10). The dots trigger sits flush right (descendant-selector specificity bump to defeat the generic `.dropdown-menu-wrap { flex: 1 }` — P76 pattern); the menu opens leftward. Actions today: Promote / Demote / Credit historical walks (all "(demo)" stubs targeting that walker). Crediting a STATIC-roster walker accrues on top of their seeded history (dynamic counts sum in `getUserShelterAffiliations` + Members tab rather than being shadowed). The fuller candidate action set — remove from pool, pause walker, coordinator notes, per-walker dog restrictions, message-via-staff — is enumerated in FC16 for the real operator surface, not built. → `features/shelters.md` (Members tab: per-walker operator stub) + FC16 (done).
6. **Surfaces show facts; provenance stays in the data** (PO call, 2026-06-10). The walker subline briefly carried up to three clauses ("64 walks · 60 credited by the shelter · tier set by shelter") — auditing information nobody reading a roster needs. Resolution: public surfaces (Members rows, profile Volunteer-work rows) render the plain total + tier pill and TRUST them; the credited/logged split and override-vs-derived distinction remain DATA-layer facts (`creditedWalkCount`, `tierOverrides` — A7's audit trail intact) that surface on the future shelter admin view (FC16), where someone actually has the auditing job. If the shelter alters something, the displayed state simply changes — no annotation about how it got there. Considered and rejected along the way: a checkmark "shelter-approved" stamp (every rostered walker is shelter-approved — a check on some rows would misread the others as unapproved) and moving the tier pill to the right rail (breaks the pill-next-to-subject convention; truncates names on mobile). → `features/shelters.md` (Members tab rendering rule) + the shelter-demo doc Beats 1 + 5 (updated in place).
