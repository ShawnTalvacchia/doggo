---
category: work-tracking
status: active
last-reviewed: 2026-06-08
tags: [questions, risks, assumptions, work-on-deck]
review-trigger: "before starting a new phase, after any strategic discussion"
---



# Open Questions & Assumptions Log

Tracks known unknowns, assumptions, and risks. Reviewed at the start and end of every phase.

**How this log works.** When a question resolves, it leaves a marker here pointing at the canonical doc that now owns it. The log is intentionally a *register* — the full reasoning lives in the SOT (strategy doc, feature doc, or phase archive), not in this file. Resolved markers exist so re-reading the log doesn't re-litigate decided questions. Long resolved blocks are fine when they capture nuance not yet absorbed by a home doc; the goal over time is to compress them down to one-liners pointing at the home.

---

## 1. Community Adoption

**Assumption:** Prague dog owners will use an app for social dog meets rather than sticking with WhatsApp/Facebook groups.

**Open:**
- What's the minimum neighbourhood density for community features to feel alive?
- ~~How do we seed initial activity without it feeling artificial?~~ **Resolved 2026-06-01.** [[Cold-Start Playbook]] is the canonical answer — three converging threads (trainer-led walks as cold-start engine, shelter dogs as always-on inventory, Doggo-as-credentialing-layer). Real PO conversations with Prague trainers + shelters are the validation prerequisite.
- What's the "aha moment" — the first experience that makes a user come back?

---

## 2. Trust & Connection Model

**Assumption:** The four-state connection model (None → Familiar → Pending → Connected) maps to real trust-building.

**Resolved:** Familiar is one-sided, silent (no notification). Connected is mutual. Post-meet review flow is the primary trigger for Familiar marking. Care services gate on Connected status. Connection model is universal (no separate provider relationships).

**Resolved (2026-04-27, Trust & Visibility Pass + Meets Deep Pass walkthrough):**

- **Action matrix v3 — Familiar gates Connect app-wide for locked viewers.** Locked viewers see only Familiar in the unmarked state; Connect appears only after marking. The trust gradient (Familiar before Connect) is now enforced in the UI. Open viewers skip Familiar entirely (it's redundant — their profile is already public).
- **Deniability principle.** Privacy of Familiar marks is preserved by deniability about the cause, not by absence of effect. When a card is bumped to Tier 2 in the receiver's view, multiple actions could explain it (bulk Familiar, profile-visibility toggle, etc.) — the receiver cannot infer specific intent. Implementation guardrails: no UI surface explains WHY a row was promoted; surface contextual signals like "you were both at last week's walk" instead. Lives in `lib/personActions.ts` (matrix), `lib/meetUtils.ts:getAttendeeTier` (tier logic, bumps on inbound `theyMarkedFamiliar`), `components/people/PersonRow.tsx` (pill suppression rules), `components/ui/ConnectionIcon.tsx` (single rendering across directions).
- **Post-meet review architecture.** Owner-forward `AttendeeActionCard` with state-grouped sections (Not Familiar / Familiar / Connected / Locked profiles), inline-pill-evolves footer pattern (Familiar → Connect → Connect ✓ + ✓Familiar/Undo footer), profile-state-aware explainer. Skip removed (section position carries the "I'm not acting on this" meaning).

**Open:**
- How many IRL interactions before users feel comfortable going Familiar → Connected?
- Will Locked users feel excluded or empowered?
- Should the post-meet review flow trigger immediately after the meet ends, or after a delay (e.g. next morning)?
- ~~**Pre-meet vs post-meet disclosure model on the People tab (P32).**~~ **Resolved 2026-05-11 (Cross-Cutting Flow Testing).** **Information** stays open across all viewers — anyone who can see a meet sees the attendee roster grouped by relationship state. **Action** is gated by *meet-level engagement* (not past attendance): viewers who are the creator, have any RSVP on any occurrence (past or future, going or interested), or follow the series can mark Familiar / Connect / Message inline. Random Discover viewers with no commitment to the meet still see info-only rows. Resolution rationale: the original past-attendance gate produced incoherent behaviour on creator-of-upcoming-meet surfaces (half-empty rows on a meet the viewer is hosting); meet RSVP is opt-in to "be in this group of people" — the meet analog of group co-membership, which already grants actions on the Group Members tab. Implementation: `viewerCanAct` in `lib/meetUtils.ts` (widened); `viewerSharedMeetWith` in `lib/mockMeets.ts` keeps the narrower past-shared-attendance semantics for the locked-profile shared-context gate (different surface, different stake). Full ratified model in [[Trust & Connection Model]] → "Meet participant visibility rules."
- ~~**Hybrid trust model.**~~ **Resolved 2026-06-01 — hybrid by design.** The shipped trust-badge architecture (community-earned > credential > platform tiers) IS the hybrid composition. §7's credentialing-layer-as-deliberate resolution formalizes the commitment. Each layer carries: **community-built** — visit reports, post-meet reviews, Familiar marks accumulating, mutual connections, repeat-client counts; **credential** — KYNOLOG.cz cert verification, Helppes alumni, methodology affiliations (LIMA / Dobrá psí škola), shelter Vetted/Experienced/Trusted Handler tier, Carer Portfolio aggregate (§8); **platform** — Verified Identity (taxonomy slot; not yet built). Fluv-inspired examples already served: "X shared connections" → mutual-connections section on profiles (PDP); "community participation score" → "Community Regular" badge in community-earned tier; "verified ID" → platform tier slot.
- ~~**Intro session (free/reduced first meeting) for direct-discovery bookings.**~~ **Closed 2026-06-01 — not a platform decision.** Providers can already edit prices and create service variants ("First session intro rate") inside the existing service-authoring affordances. No platform-level toggle needed. Same close applies to §6's "Intro session as a proposal toggle" entry.
- **Soft Familiar indicator across the app (P29).** Showing "you've marked this person Familiar" on profile cards / group members / attendee lists — privacy-safe (your own data), but a real product call about how visible to make the user's own relationship state.
- **Bulk "Mark everyone Familiar" on group Members tab.** Convenience optimization for routine groups where the user knows everyone. Tension: the Familiar mark's value is its deliberateness — it means "I recognize this person." Bulk-marking everyone in a group dilutes that signal, and applies expanded profile-visibility grants to people the user may have never met. The post-meet review's bulk-mark works because attendance IS the shared trust event; group membership doesn't carry that same per-person signal. Two implementation paths if pursued: (a) dynamic auto-marking — new members who join later are auto-Familiar-ed (privacy concern: granting visibility to future strangers); (b) snapshot — only current members at click time get marked, new members miss the bulk action. Both have drawbacks. Defer until we have more data on how users actually want this to behave; revisit during user testing or after Mock World Building when we can dogfood the manual per-row flow more.
- ~~**Inquiry-driven trust transitions (transactional path to Familiar/Connected).**~~ **Resolved 2026-05-08 (Inbox & Notifications B1–B2).** Four-rule model: **(1) Inquiry send → mutual Familiar** (`InquiryFormModal.handleSubmit`); **(2) Contract sign → mutual Connected** (`ThreadClient.handleSign` + booking detail `handleSign` via `markConnected`); **(3) First message in any non-Connected conversation → mutual Familiar** (`ThreadClient.handleSend` — covers Appointment-flow "Ask about this" path); **(4) Withdrawn / declined doesn't roll back.** Profile-hero CTA implication shipped 2026-05-05: Provider-tier profiles' primary action is "Book care" (replaces Connect); Helper-tier keeps Connect. Full ratified model in [[Trust & Connection Model]] → "Inquiry-driven transitions."

---

## 3. Groups & Meets

**Assumption:** Groups are the right persistent container for community — meets alone aren't enough.

**Resolved:** Four group types (park/neighbor/interest/care). Park groups auto-generated. Group detail tabs vary by type. No Chat tab on groups — Feed with flat comments for async (built), meet-level chat for real-time coordination (built). Going/Interested RSVP states with interactive cycle (Going/Interested/Leave). Meet detail tabs (Details · People · Chat) — built with photo gallery. Schedule tabs: Upcoming / Care / Interested. Care tab has sub-filter pills (All / Getting Care / Providing). Interested tab auto-populates from joined groups. Discover flow refactored: hub → results with FilterPillRow + floating Filters button (no more type picker pages). Care group discovery path resolved: Discover > Dog Care with filter pills for service type.

**Resolved (2026-04-27):** Recurring-meet RSVP model. `Meet.cadence` (`one_off | weekly | biweekly | monthly`) is the discriminator. For recurring series, RSVP is always per-occurrence: `Meet.attendeesByDate` keyed by ISO date holds the authoritative roster, with each upcoming row offering Going + Skip (Skip persists via `useDismissedReviews`, mutes-in-place with inline Undo). Series-level subscription is a separate "Interested" toggle (data on `Meet.followers`). Per-occurrence Interested was considered and dropped — "maybe to a specific Wednesday" added noise without much value. Schedule renders one card per (meet, occurrence-date). Cards across the app show "Next: [date]" for recurring meets. See `archive/phases/meet-recurrence-model.md`.

**Resolved (2026-05-05):** Per-occurrence cancellation for recurring meets. `Meet.cancelledDates: Record<ISO, { reason; cancelledAt }>` mirrors the booking-side shape. Host-side affordance: a quiet "Cancel" link next to the Hosting pill on each meet-detail Upcoming dates row; tap opens `CancelOccurrenceModal` (reason required). Cancelled rows render muted with strikethrough + reason caption; attendees see a "Cancelled" pill, host sees a Restore link. Schedule keeps cancelled occurrences visible on Upcoming with muted treatment + reason caption (calendar-app convention: don't silently disappear), and filters them out of the History review pipeline. Cancel vs Skip distinction: cancel is a host action affecting all attendees and supersedes Skip; Skip stays a per-user opt-out. Out of scope: per-occurrence editing (cancel kills, doesn't reschedule), broadcast notification when a host cancels (belongs to Inbox & Notifications). See `docs/features/meets.md` → Recurrence model.

**Open:**
- Can the platform suggest meets based on patterns? ("3 regulars walk near Letná on Tuesdays")
- What's the right max group size for different meet types?
- Should meet discovery include a map view?
- Park group scaling — what happens when a park group hits 500 members?
- Can neighborhood groups be auto-suggested based on user density?
- **Cross-category groups (partially resolved).** A trainer running sessions at a specific park is a Care group with the park as its context. Recurring park meets surface in Parks too via the service-intersection rule (see `Groups & Care Model.md` → Care Group Admin Model). Open: exact rules for when a Care group's meets appear in Parks search vs only in Dog Care search.
- **Group-to-group crossover meets.** A trainer co-hosting a workshop between their Care group and the reactive dog support group — how does this work? Dual-listed? Shared event?
- **Series subscription notifications.** Following a recurring series should produce notifications when new dates are added to a series with an irregular cadence and possibly 24h-before reminders for upcoming occurrences. The `meet_series_update` notification type is wired (stubbed entry on meet-7), but real triggering, batching, and opt-out behaviour are deferred. Belongs with the broader notifications work — not Mock World Building.
- **Series ending semantics.** Recurring meets carry an optional `seriesEndDate`, but UI for end-of-series (last occurrence past, no further dates) is unbuilt. Treat as inactive series for now; design when a real series in mock data hits its end date.
- **Group admin controls for meet creation.** Today any group member can create a meet in any group they belong to (including private/approval groups). For some groups that's right (Vinohrady Evening Walkers benefits from any regular calling a walk); for others it's a problem (a Care group's meets are part of the provider's offering, not random member events). Open: should each group have an "anyone / admins / approval-required" setting on meet creation? Should there be type-level constraints (e.g. Care groups → admin-only by default; Interest support groups → approval; Park/Neighbour → anyone)? Should admins be able to set per-meet constraints (max size, leash policy, reactive-dog-friendly tags) that all member-created meets in the group inherit? Long-term — not blocking demo. Surfaced 2026-04-30 during meet-create flow walkthrough on Vinohrady Morning Crew.
- **Group visibility chip clarity.** `group.visibility` is a single enum (`open | approval | private`) — three mutually exclusive states on a continuum: open (visible + free join), approval (visible + gated join), private (hidden + gated join). Current chip rendering: open → no chip, approval → "Approval required", private → "Private". Each label carries half the meaning: "Approval required" doesn't communicate that content is still visible to non-members; "Private" doesn't communicate that joining still requires a request. Together with the description users mostly figure it out, but it's a teaching moment that's underdone. Possible refinements: "Approval to join" (action-y), "Private · approval to join" (combine in chip), or hover/tap tooltip with the explainer. Belongs in the Onboarding & In-Product Communication phase (currently paused — needs rewrite per 2026-06-01 close note; this item waits with it). Surfaced 2026-04-30 during Mock World Building C7 walkthrough.
- ~~**Care group provider presence — community vs. marketplace tension.**~~ **Resolved 2026-05-04 (Discover & Care B1–B4):** Care-group hero anatomy decided — restrained provider attribution (avatar + name + category badge + 1–2 community-earned trust badges + "View profile" button + tagline from carer bio for single-provider, suppressed for multi). Multi-provider support shipped via `Group.providers: GroupProviderRef[]` (replaces single `hostedBy: string`); avatar stack with "+ N" tail; primary provider for "View profile" routing. Service-intersection rule passes through as a helper (`getCareGroupServices`) — full per-service location/methodology metadata is future work. See [[Groups & Care Model]] → Care Group Admin Model + Care-group hero anatomy.
- **Meet location — structured place selection.** Meet location is free-text today. `Meet.lat` / `Meet.lng` exist on the type but aren't collected, so attendees can't tap for directions and there's no way to disambiguate (Stromovka big field vs east gate). Three paths: (a) Prague-specific POI autocomplete from a curated list of 40-60 dog parks + tram stops + addresses — backfills lat/lng, no API dependency, feels locally considered; (b) full map picker (Google/Mapbox); (c) text + optional drop-a-pin. Care groups at fixed locations have the same need. Belongs to a Geo & Discovery pass or Discover & Care follow-up. Surfaced 2026-04-25.
- **Frequent-attendee signal on recurring meet People tabs.** All-attendees view lists everyone who's ever been associated with the series flatly — no signal that someone came to 5 of the last 6 walks vs. one walk a year ago. Options: (a) "Regular" pill next to the name; (b) attendance-frequency line ("Came to 5 of last 6"); (c) sort priority — regulars first within each relationship-state section; (d) some combination. Pairs with the soft Familiar indicator (E-stream, Discover & Care). Would need a `getAttendanceFrequency(meet, userId)` helper. Surfaced 2026-04-29.
- **Dog-to-dog tagging at meets.** Surfaced 2026-05-08 during Inbox & Notifications walkthrough (E2). The post-meet review flow already lets attendees tag each other (people-to-people Familiar marks). The community-first thesis ("dogs meet at meets → trust → care") implies a parallel layer for dogs themselves: which other dogs did mine play with? Use cases: (a) trust signal — owners see "Bára's met Toby 4 times now"; (b) provider notes — trainer logs which group dogs were present and how they interacted; (c) pet profile enrichment — "frequent playmates" section. Visit reports (provider seals after 1-on-1 or group session) could include a "dogs encountered" field that pulls from the meet roster + free-add. Implementation cost is real: new `petConnections` data layer (parallel to user `connections`), UI in post-meet review for tagging dogs, pet-profile section to surface accumulated relationships. Strategic question worth probing: does this *strengthen* the community→care thesis (dogs forming relationships becomes another trust currency) or *complicate* it (now there are two parallel social graphs)? Recommendation: defer until post-Demo. Demo presentation can hand-wave with seeded data ("Bára has 4 frequent playmates at Stromovka") to test the appeal before building the tagging UI. Belongs to a future Pet-Social Graph phase or folded into a Trust & Privacy refinement. Surfaced 2026-05-08.

---

## 4. Provider Model

**Assumption:** Users are comfortable evolving owner → helper → provider when framed as community contribution. No separate provider identity — it's a dial, not a switch.

**Resolved:** No separate provider signup. Provider toggle from existing profile. Same layout with additional sections. Care groups created by providers with config options per category. Rolling weekly billing model for recurring bookings — sessions generate one at a time, no fixed session count. Provider session actions: Start → Complete → Add note. Booking detail page works for both owner and provider perspectives with tabbed layout (Info / Sessions / Chat).

**Resolved (2026-05-02 — Services-as-catalog model):** Services is the umbrella label on a provider's profile — the comprehensive catalogue of all paid offerings. Under it, two offering types distinguished by what booking produces: **Care-type** (Walking / Sitting / Boarding — drop-off, owner doesn't sign up to a specific time, booking produces a Booking record) and **Meet-type** (Training sessions / Workshops / paid Group walks — owner signs up to a specific time, booking produces a Meet attendance). Test: "does someone sign up to a specific time?" — yes = Meet, no = Care. Bio-level mention is fine ("I work on basic recall during my walks" inside a walking-service description is marketing inside Care, not a separate Training service). Profile Services tab is comprehensive — both types listed in one tab; tap routing differs by offering type. Full spec in [[Groups & Care Model]] → Services as Catalog.

**Open:**
- At what point does "helping" feel like a job?
- ~~Where does pricing surface before a formal arrangement?~~ **Resolved 2026-05-05 (Pricing & Proposals).** Per-service pricing on Discover Care cards (`ProviderCard.pricesByService`); live estimates on InquiryForm; same engine-computed quote at three surfaces (InquiryForm estimate, InquiryCard estimate, ProposalForm System quote). See `lib/pricing.ts` + `archive/phases/pricing-and-proposals.md`.
- Should creating a Care group require having services listed first?
- Should providers be able to set cancellation policies (e.g. 24hr notice)?
- ~~What does the session completion flow look like for providers?~~ **Resolved 2026-05-08 (Sessions & Service Execution).** Visit report card with photos, notes, structured care checks (kept as dormant type field — see Care checks note below), walk metrics (distance + duration for walks), `completedAt` timestamp. Provider seals via single-tap Finish on the Active panel; Undo affordance pre-seal covers the only real failure mode. Edit-after-submit deferred to Inbox & Notifications.
- ~~Should owners receive real-time updates during active care sessions?~~ **Resolved 2026-05-08.** Yes — mid-session photos + (for walks) GPS-stub tracking with live km/min readout. Owner sees a passive Active panel with the latest mid-session photo + reassuring copy; no composition fields. Live GPS is stubbed (simulated tick); real Realtime delivery is post-demo.
- ~~What pet info should surface in the provider's "in-session" view?~~ **Resolved 2026-05-08.** `PetInfoSection` on the Info tab (provider-only) renders medications, conditions, "around dogs" socialisation notes, and vet (clinic + phone). Collapsed by default, avatar + name + breed/weight/age visible at the collapsed level for at-a-glance recognition. Bumped to 48px rounded-square (Rule B — dogs are rounded squares).
- **Care checks — dormant 2026-05-07.** Originally part of the visit report (Fed / Watered / Walked / Pottied / Meds Given chips) per the Sessions & Service Execution A1 spec. UI removed during walkthrough — too tautological (every walking session has *Walked: ✓*; every boarding has all four true), so chips were friction without payoff for both provider (one more thing to tap) and owner (visual noise on every report). Type field `VisitReport.checks` kept as optional + dormant; stripped from seeded reports (`kd-5`, `s-olga-6`). Open: do they earn their keep with a richer hook, or get removed entirely? Three paths if we revisit: **(a)** bind to specific pet-info entries — e.g. Bára has *Trazodone before high-stim walks*, so a single `Meds given · Trazodone before walk` row appears only when the dog has that med; **(b)** boarding-only feeding + medication log with timestamps (closer to Time To Pet's check-in shape than a chip row); **(c)** cut the field entirely once we've validated 3+ phases of demos without anyone missing them. Decide before Demo Presentation phase or roll forward.
- **Multi-leg tracking within a single session.** Today GPS tracking is one-shot per session — once Stop is tapped (or Finish auto-stops), the metrics seal and the "Log route with GPS" button hides. Re-tracking-and-overriding wasn't worth the prototype complexity. But real bookings will have shapes where one care session spans multiple discrete activities: morning walk → mid-day potty break → evening walk, or walk → playtime → walk. Forcing the carer to create a new booking for each leg is wrong — it's one contractual relationship with the owner. Resolution sketch: `report.walkLegs?: Array<{ distanceKm; durationMin; startedAt; endedAt }>` rather than a single pair, with the visit-report renderer summing legs for the headline figure. UI implication: after Stop, the "Log route with GPS" button comes back as "Track another leg" with the running tally visible. Defer until past first-round demo; surfaced 2026-05-07 during B7 walkthrough.
- **Note + photo together — composer for in-session updates.** Surfaced 2026-05-08 during the active-session sub-page walkthrough. Today on the Active panel, photos and notes are independent: tap "Add a photo" → file picker → photo lands silently in the row; tap "Add a note" → textarea expands inline. There's no way to attach a caption to a specific photo, or to send "photo + caption" as one update artifact. The natural composer pattern is: tap "Add a photo" → file picker → after pick, a small inline form appears with the image + a one-liner caption field + Send. Caption optional — empty just commits the photo as today. Implementation cost: a transient "photo+caption draft" state in ActiveSessionPanel (pre-commit), the small inline form UI, persistence of the caption alongside the photo URL on `VisitReport.photos`. Probably wants `photos: Array<{ url: string; caption?: string }>` instead of `string[]` — that's a small migration. Belongs to a future Sessions polish pass; the standalone Add a note action stays regardless. Surfaced 2026-05-08.
- **Multi-active-session banner — provider with concurrent bookings.** Today the active-session banner (mobile strip / desktop sidebar item) is wired to a single active session — pet thumbnail + "Sitting Bára" + chevron-to-that-booking. Real providers boarding 3 different owners' dogs at once would have 3 simultaneous in-progress sessions, and the banner can't speak in plural. Adaptation isn't tricky structurally: when `activeSessions.length > 1`, banner copy switches to "3 active sessions · {summary}", thumbnail stack replaces the single avatar, and the click target routes to `/bookings` (filtered to in-progress) rather than a single booking detail. The booking-detail Active panel itself stays per-booking — only the across-the-app presence indicator needs the plural treatment. Defer until past first-round demo; surfaced 2026-05-07 during B7 walkthrough.
- ~~**Lock vs. Tier reconciliation.**~~ **Resolved 2026-04-29 (Community & Groups Deep Pass F1); reframed 2026-05-10 (Discover Refinement A):** Lock = visibility (privacy axis — who can see the profile). What was previously called "Tier" (the action axis) collapsed during Discover Refinement into a single boolean on the Carer's profile (`carerProfile.publicProfile`) — the action axis remains, but it's no longer a separate tier classification, just an audience setting on the one Carer role. The Lock + Carer-audience composition matrix lives at `features/profiles.md` → "Lock + Carer audience: two settings, one role."
- ~~**Helper vs Carer terminology for the casual tier.**~~ **Resolved 2026-05-10 (Discover Refinement opening discussion):** Helper retired entirely; Carer becomes the only role noun. The Provider tier label retired with it — what was previously "Helper" / "Provider" collapses into one Carer role with an audience setting (`carerProfile.publicProfile` — circle vs anyone). Same person, same role, wider audience as the dial moves up. Per-row tier badge on PersonRow deleted; Carer audience signaling lives at the surface level (Discover Care section structure, profile hero "Open to bookings" status pill if it earns its keep). Data field `publicProfile` retained as the underlying setting. Doc cascade landed: [[Groups & Care Model]] "Carers on Profiles," [[Trust & Connection Model]] "How visibility applies to Carers," [[profiles]] "Lock + Carer audience: two settings, one role," [[badges]] "Two categories" (Tier removed), [[User Archetypes]] "The Carer Dial" + Casual/Aspiring/Professional **Carer** archetypes. CSS classes `.person-row-pill--helper` / `--provider` / `--care` deleted; `careTier` prop removed from `PersonRow` + `ParticipantList` + `MembersTab`; `components/people/TierBadge.tsx` deleted.
- **In-product communication of the Services / Care / Meet model.** The data model has a clear split (Care-type vs Meet-type offerings — see [[Groups & Care Model]] → Services as Catalog). The user-facing copy doesn't yet teach this. Two surfaces that need design work: **(1) Provider onboarding when adding an offering** — the routing question ("How does this work? — I take the dog (Walking/Sitting/Boarding) — I run a session people sign up for (Training/Workshop)") needs UX treatment so providers fall into the right shape without seeing data-model jargon. **(2) Owner-facing card differentiation** — Care cards (price-per-visit, availability days, "Request booking") vs Meet cards (next-upcoming-date, "Book a spot"). Same Services tab surface, different card behaviour. No labelled "Care vs Meet" segmentation — the card content does the work. Belongs in the Onboarding & In-Product Communication phase. Surfaced 2026-05-02 during Mock World Building C11 walkthrough.
- ~~**Vet & grooming offerings — neither pure Care nor pure Meet.**~~ **Resolved 2026-05-02 (Discover & Care C1):** Third **Appointment-type** offering shape. `CarerAppointmentServiceConfig` carries `appointmentCategory: "vet" | "grooming"`, fixed `pricePerAppointment` and `durationMinutes`. Two test questions distinguish all three offering types: *Does someone sign up to a specific time?* (No → Care. Yes → Meet or Appointment.) *Are there other dogs / a roster?* (Yes → Meet. No → Appointment.) Tap routing: Appointment → request appointment / slot picker (chat-based for prototype). See [[Groups & Care Model]] → Services as Catalog.
- ~~**Meet visibility level: `participants_only`.**~~ **Resolved 2026-05-02 (Discover & Care A1):** Added `"participants_only"` to `MeetVisibility`. `isMeetVisibleTo(meet, viewerId)` helper in `lib/meetUtils.ts` enforces — `participants_only` is visible only to creator + attendees. `getGroupMeets` filters accordingly. Backfilled `meet-care-6` (Hana's 1-on-1 with Klára) so it stops leaking onto the public Klára-group Meets tab. The "auto-apply when package generates an instance" behaviour is groundwork for Pricing & Proposals; today `participants_only` is set explicitly on seeded contracted instances.
- **Per-service visibility (third axis).** Today the trust model has two orthogonal axes: **Lock** (profile visibility — who can see) and **Tier** (Helper / Provider — who can act on services). A natural extension is a third axis: per-service visibility, where a Helper-tier user offers Service A to all Connected viewers but Service B only to a curated subset. Use case: Tereza is comfortable boarding for her three closest neighbours but doesn't want her general Connected network seeing the boarding offer. Sketch: each `CarerServiceConfig` gets `visibilityScope: "all_connected" | "specific" | "group"` and an allowlist; service cards filter per viewer; "Book a session" CTA only renders for eligible viewers. Pairs with the existing Helper/Connected gate. Belongs to a future Trust & Privacy refinement. Surfaced 2026-05-02.
- **Multi-pet booking treatment.** Surfaced 2026-05-08 during Sessions & Service Execution close. The pet-as-protagonist hero (`SessionsPetHeader`), the booking-list row pet avatar, and the `PetInfoSection` all currently assume a single pet. For two pets: avatar stack + "Bára & Hugo" name treatment is the natural pattern; for 3+ pets: avatar stack + "Bára + 2" tail. No multi-pet booking is seeded in mock data today, so the design is deferred until a real one lands. Belongs to whichever phase first hits a multi-pet flow (likely Onboarding & In-Product Communication or a focused side task during demo prep).
- **Provider-replaceable booking hero photo.** Surfaced 2026-05-08 during Inbox & Notifications walkthrough. Today the Sessions-tab hero (`SessionsPetHeader`) uses the owner-uploaded `pet.imageUrl` as the booking's visual centerpiece. For repeat / long-running arrangements (training series, recurring boarding) this could be a meaningful personalization point: the provider replaces the hero with a photo of *them* with the dog — taken during a session, mid-walk, on the porch — turning the booking page into a shared artifact that reflects the actual relationship rather than a static profile photo. Both sides (owner + provider) see the same updated hero. UI sketch: small "Replace photo" affordance visible to the provider on the Sessions tab, surfaced only on `type: "ongoing"` bookings (one-offs aren't worth the friction). Persistence: `Booking.heroPhotoUrl?: string` overrides the pet default. Behavioral nudge: providers organically take care photos already; this gives those photos a natural home AND signals to the owner "your dog is in good hands with someone who's invested in this." Belongs to a future Demo Presentation polish pass or a focused side task during demo prep — not blocking.
- ~~**Discover Care surface gaps cluster.**~~ **Resolved 2026-05-10 (Discover Refinement closed).** All five gaps shipped: community-first ordering on `/discover/care` (Connected/Familiar Carers above broader marketplace, distinct chrome), Appointment filter pill (with vet sunset → grooming repurpose), ProviderCard ↔ UserProfile bridging (all 12 directory entries now bridge to real users), per-service pricing resolution (`resolveDisplayPrice` reads through bridge per active filter), service-aware filter fields shipped (`pace` / `leashPolicy` / `homeType` / `hasOwnDogs` / `hasYard` / `maxDogs` on `CarerCareServiceConfig`; predicates wired in `applyAllFilters`). Plus: Helper/Provider tier nouns retired entirely → single Carer role with `publicProfile` audience setting; Carer Identity badge with sub-spec resolution + privacy gate; avatar Rule B sweep app-wide (P58 closed). Filter-panel UX redesign + service-taxonomy resolution split out into the next phase (see "Care service taxonomy + filter cluster" below). Phase board archived to `docs/archive/phases/discover-refinement.md`.
- ~~**Care service taxonomy + filter cluster.**~~ **Resolved 2026-05-11 (Care Catalog Taxonomy & Filter Redesign closed).** Four-service taxonomy shipped — `walks_checkins` / `house_sitting` / `day_care` / `boarding` — with documented meanings inline in `lib/types.ts:ServiceType` JSDoc to prevent re-drift. Drop-in visits moved to `house_sitting` (short version of same shape — carer goes to owner's home, finite duration). Price unit kept flexible: house sitting defaults per-visit but supports per-night; walks + day care per-visit; boarding per-night. Mock data migration normalized 9 drifted sub-service strings (`"Home boarding"`, `"Training walk"`, `"Adventure walk"`, `["Home boarding", "Day care"]`) to canonical `SUB_SERVICES` map in `lib/constants/services.ts`. Filter panel redesign shipped — `Filters` heading + in-panel service-type dropdown with descriptive sublines; Pets + Nearby + Sub-services all functional; service-aware fields keyed off active pill; filters persist per-viewer via `usePersistedState`. See `archive/phases/care-catalog-taxonomy-and-filter-redesign.md` + [[features/explore-and-care]] for full spec.

---

## 5. Safety & Liability

**Open:**
- Platform liability during meets?
- Does blocking someone remove you from shared public meets/groups?
- What incidents require trust regression (removing Connected status)?

---

## 6. Monetization

**Assumption:** Trust must exist before monetization. Community features stay free. Pet service businesses (training, walking, grooming, boarding, rehab, venue, vet) are represented on Doggo as Care groups. Pet product retailers and other non-service adjacent businesses are not Doggo members.

**Open:**
- What user behaviour indicates readiness for paid transactions?
- Platform-mediated payment from the start, or peer-to-peer first?
- Do Prague pet businesses see value in a community-embedded directory?
- **Adjacent-business advertising (post-MVP).** Can localized promotional placements for adjacent businesses (salons that don't want a Care group, toy shops, pet food retailers) be a post-MVP revenue line that points to external sites? What's the right gate — max one promo per session, per surface, user-toggleable? Does advertising threaten the community trust thesis if not carefully framed? *Vets specifically may belong here rather than as a first-class account type — see "Provider category prioritization" below. See also: [[Shelter Dogs & Community Walks]] for an adjacent thread on non-paid account types that could feed cold-start.*
- **Group revenue share (post-MVP).** Can Care group admins take a small cooperative-fee percentage of member bookings? See Groups & Care Model: Care Group Admin Model.
- **Provider discounts, including group-anchored ones.** Providers should be able to set discounts on their services — flat or percentage — including auto-applied discounts tied to group membership. Strongest demo case: a trainer offers 15% off services to members of their Care group ("join the group, get the discount" — community → care funnel made explicit). Open: where do discounts live (per-service field vs. separate `discounts: Discount[]` array on `CarerProfile`), how are they communicated (badge on service card vs. line item on the proposal), can owners stack them, do they have expiry / usage caps. Pairs with intro session offers. Belongs to a future Bookings & Monetisation pass. Surfaced 2026-05-02.
- ~~**Intro session as a proposal toggle.**~~ **Closed 2026-06-01 — not a platform decision.** Providers can already create service variants ("First session intro rate") inside the existing service-authoring affordances; no platform-level toggle needed. See §2's parallel close.
- **Structured pricing model + no-bargaining principle (RESOLVED 2026-05-05 — shipped in Pricing & Proposals).** Auto-pricing engine (`computeQuote(config, inquiry, today)` in `lib/pricing.ts`) takes config × inquiry data → quote. Starter modifier set (4): Holiday surcharge, Weekend rate, Multi-pet, Last-minute — all working with already-captured inquiry data. ProposalForm refactored from "compose price" to "review system quote and confirm." Override stays available but is visually flagged on the owner's `BookingProposalCard` with optional reason. Engine output visible at three surfaces (InquiryForm live estimate, InquiryCard estimate, ProposalForm System quote) — same number throughout the lifecycle. Per-service pricing on Discover Care cards via `ProviderCard.pricesByService`. Provider modifier-config UI in `ProfileServicesTab` (`PricingModifiersEditor` accordion). **Sub-questions surfaced during the phase, deferred to a future modifier pass:**
  - **Ongoing-vs-one-off pricing divergence.** Engine v1 deliberately skips Holiday + Last-minute on ongoing bookings (rolling weekly billing handles each week independently). Is that the right model long-term, or should ongoing surcharges apply to occurrences that fall on holidays even if the cycle total is averaged? Surfaced 2026-05-05.
  - **Holiday line item granularity.** Today's engine lumps multiple holidays into a single "+30%" line with a trigger note like "3 Czech public holidays fall in this booking." Should that split into per-holiday lines so the math is even more legible? Surfaced 2026-05-05.
  - **Future modifier dimensions** (deferred from v1; need inquiry-form expansion): longer-walk (`durationMinutes`), off-hours (time-of-day), boarding-specific (yard, house type, max-dogs-at-once), add-on opt-ins (bath, grooming, photo updates), package selection (5-night bundle).
  - **Discount + intro-session integration.** The two entries above (provider discounts; intro session toggle) still belong to a future Bookings & Monetisation pass — they're additive to the modifier engine, not replacing it.

---

## 7. Prague, Cold Start & Go-to-Market

**Assumption:** Prague dog culture values routine, parks, informal socialisation. Seeding with providers who use meets as a client acquisition channel solves both sides of the marketplace.

**Refs:** `Competitive Research - Prague Dog Care Scene.md` (trust patterns, badge taxonomy, seeding strategy)

**Open:**
- Need to map Czech-language informal networks (Facebook groups, forums) beyond the English expat ones we've found
- What expectations exist around verification/identity in Czech culture?
- Are Vinohrady/Letná/Vršovice the densest neighbourhoods for dog owners?
- Can we recruit 3-5 providers willing to host weekly meets? What's the pitch?
- What's the ROI timeline — how many weeks before meet attendees convert to paying clients?
- Does provider-hosted meets feel organic or salesy?
- Should Doggo offer seed provider incentives (free period, reduced fee, featured placement)?
- ~~**Provider category prioritization.**~~ **Resolved 2026-05-11 (Care Catalog Taxonomy).** Walks + training prioritized for cold-start (Cold-Start Playbook); grooming follows; boarding / day-care / house-sitting come as platform matures. Vets retired from `AppointmentCategory` + `CareCategory` enums entirely (vet PMS software is sticky + compliance-integrated; vet selection isn't community-shaped). Pet-side `VetInfo` (owner records their dog's vet for medical record-keeping) retained as separate concept. Adjacent: vets may belong in Adjacent-business advertising (§6); see [[Cold-Start Playbook]].

- ~~**Marketplace-with-community vs. community-with-marketplace strategic fork.**~~ **Resolved 2026-06-01 — reframed as false binary.** The original two-option framing forces a "primary thing / secondary differentiator" shape that doesn't describe what Doggo is. Doggo is a **dog ecosystem platform** where community and care surfaces are inseparable and mutually reinforce through the credentialing layer (community-earned trust signals make care trustworthy; care surfaces consume and reward those signals). Past decisions that looked "community-favored" (Discover Care community-first ordering; the framing principle in [[Cold-Start Playbook]] → trainer-led walks as community walks where the host happens to be a trainer; shelter walker design without leaderboards) and decisions that look "marketplace-favored" (Pricing & Proposals, Carer Portfolio, trust badges, mutual Connected on contract sign) are *both* expressions of the same underlying ecosystem-platform shape. **Operating principle:** community ↔ marketplace tensions surface at the most strategically important decisions and get **careful case-by-case deliberation** — not pre-committed rules. These are high-leverage strategic decisions; they don't get made reactively. **Future options preserved:** premium community features, gated tiers, paid-only community surfaces — none ruled out; none chased now. Revisit if a specific decision surfaces real tension that the case-by-case deliberation can't resolve.

- ~~**Doggo-as-credentialing-layer for Prague's training market.**~~ **Resolved 2026-06-01 as deliberate product direction.** Credentialing layer is the strategic moat, not an emergent property. Already in motion: trust-badge architecture (community / credential / platform tiers), engagement-based aggregation rule (§8 — abuse-resistant), the **merged Carer Portfolio + Shelter Walker Credentialing phase** (2026-06-01 strategy conversation) explicitly as moat-investment. **Forward commitments** (when the moment lands, post-demo): active partnerships with credentialing bodies (KYNOLOG.cz, Helppes, Dobrá psí škola network), in-person shelter vouching as a credential pipeline, "trust infrastructure for Prague's dog ecosystem" as the external pitch framing. **Protected:** badge integrity — no sold badges, no inflated thresholds for growth, engagement-only aggregation. Refs: [[Cold-Start Playbook]] → "The credentialing-gap moat."

- **Trainer-positioning validation gate** *(surfaced 2026-05-18 during Demo Narrative V2)*. The V2 guided demo is being built as a concept-test + recruitment instrument aimed first at **walker-trainers** — the cold-start engine. The gate: real conversations with local Prague walker-trainers, pitching the positioning (the public walk as funnel + training as the on-ramp to community — see [[Product Vision]] → "Trainer-Led Walks & the Training Value Proposition"). If it lands — they see why they'd use the platform and would help populate it — that's the signal to take the project seriously and plan next steps (resourcing, real backend, cold-start execution). Without trainer buy-in the community side has no seed and likely never reaches critical mass. Builds on the PO briefing §4 validation moves + [[Cold-Start Playbook]] → validation prerequisites. Revisit at the V2 strategic review.

---

## 8. Trust Badges & Hybrid Trust

**Assumption:** Community-earned trust is primary, but lightweight top-down signals (verified ID, network overlap, credentials) bridge the gap for users who haven't built connections yet.

**Refs:** `Competitive Research - Prague Dog Care Scene.md` (badge taxonomy), `Competitive Research - Fluv.md` (hybrid trust model)

**Resolved (Profiles Deep Pass walkthrough, 2026-05-11):** **Carer portfolio aggregation sources from completed engagements, not photo tags.** A trust signal like "Klára has trained 30 dogs" computes from verifiable engagement records — `Booking` records where `state === "completed"` plus `MeetAttendee` records on past meets the carer hosted — NOT from how many posts tagged the carer or her dogs. Two reasons: (1) **two-sided ground truth** — the owner explicitly engaged, the carer delivered, the record exists; consent is embedded in the engagement act itself (no separate "do you consent to your dog appearing in someone's portfolio?" question). (2) **abuse-resistant** — a bad actor can't fake a portfolio by photo-tagging dogs they never cared for. Photo tags stay scoped to their own job (content/communication/memory). Aligns with the Cold-Start Playbook credentialing-layer thesis — verifiable, hard to fake, accumulates with real work.

**Open:**
- Which badges matter most to owners for MVP? Prioritise from the full taxonomy.
- Self-declared credentials only at launch, or verify any?
- How do badges display on compact cards vs. full profiles? Max count before clutter?
- Does "Trusted by your network" require Connected, or does Familiar count?
- Should there be an "intro session" (free/reduced) for the direct-discovery booking path?
- ~~**Carer Portfolio badge — threshold + display rules.**~~ **Resolved 2026-06-01.** Five sub-questions answered: **(a) Threshold** — badge shows from 3+ completed sessions; below that, no badge (avoid 1-session noise). Three visual states tied to count thresholds: 3–9 entry (outlined), 10–24 established (filled), 25+ seasoned (filled + ring/accent). **(b) Surface** — extends the existing `TrustBadgeStrip` on the carer profile hero (no dedicated card); propagates to PersonRow + Discover cards per the existing priority rule (top 1 / top 2 slots). Visual state shows everywhere the badge appears. **(c) Copy format** — `"{tier-label} · {N} sessions"`. Labels: "Carer" (3–9) / "Experienced Carer" (10–24) / "Trusted Carer" (25+). Rejected "Trained X dogs" (framing-loaded; assumes training; doesn't generalize) and "X dogs · Y sessions" (visually busy; dog count is genuinely complex to compute across multi-pet bookings + meet attendance). **(d) Time scope** — all-time for V1. Accumulation should persist — that's the point of credentialing. If testers raise "still active?", surface as a separate "Last active" line on the profile, not folded into the badge. **(e) Unit naming** — "sessions" everywhere user-facing (booked Care visit = 1, hosted Meet occurrence = 1, Appointment booking = 1). Code-internal aggregate keeps whatever name fits `lib/trustBadges.ts`. **Cross-thread:** shelter walker tier badges (Vetted Walker / Experienced Walker / Trusted Handler — per-shelter institutional credential) share the same visual escalation language (outlined → filled → filled+ring) and the same TrustBadgeStrip placement. Same credentialing-moat thesis applied to two domains — strong design justification for **merging Carer Portfolio + Shelter Walker Credentialing into one phase** when it opens (per the 2026-06-01 strategy conversation; ROADMAP update pending). Implementation remains mechanical: `lib/trustBadges.ts` aggregator + `TrustBadgeStrip` extension + mock data sanity check.

---

## 9. Navigation & UX

**Resolved:** Mobile bottom nav: Community | Discover | My Schedule | Bookings | Profile (5 tabs). Desktop sidebar: Community, Discover, My Schedule, Bookings, Inbox, Notifications, Profile (7 items). Mobile header: create + notifications + inbox. Home renamed to Community with category sub-tabs. All pages unified to PageColumn single-column layout (640px centered). Sidebar tightened to 180px. MasterDetailShell and DiscoverShell deleted. Sidebar active state: neutral (transparent-dark-4 + text-primary). Header action buttons: CTA pill variant (brand for primary, outline for secondary). DetailHeader and PageColumn headers aligned (40px, 16px padding). Scroll-to-hide nav restored via page-column-panel-body class. Bottom-nav visibility regex updated to keep nav on hub-route sub-tabs (`/schedule?view=meets`, `/profile?tab=posts`, `/bookings?tab=services`) — fixed during Persona Wiring.

**Resolved:** New-user feed populated by `getNewUserFeed()` — picks public/nearby content, completed-meet recaps from open groups, "find your park" prompts. Switcher persona `new-user` previews this state across every surface (`useIsNewUser()`). Closed by Persona & Demo Mode Wiring (2026-04-26).

**Open:**
- **Terminology consistency sweep.** Concepts with naming conventions; some resolved, some open:
  - **Private vs Locked** — surfaced 2026-04-30. Data model uses `profileVisibility: "open" | "locked"`. UX copy + walkthrough standardise on **Private** / **Public** (gentler, matches Instagram/Threads conventions). **Profile surfaces swept Profiles Deep Pass 2026-05-11** — visibility chip, picker setting, hero chip, Familiar-inclusive description all updated. Surface-side drift closed. **Still open:** code field rename from `"open" | "locked"` → `"public" | "private"` deferred (mechanical rename, ~30 callsites, low priority).
  - ~~**Groups vs Communities.**~~ **Resolved 2026-06-01.** "**Communities**" is the user-facing word — nav label, route (`/communities/[id]`), conversational copy ("post in this community"). "**Group**" is the data noun in code only — the type, the schema field (`groupType`), internal identifiers. External UI copy uses "community" / "communities" exclusively; never "group" / "groups" in user-facing strings. Code stays on `Group` for consistency with the typed data shape — costly rename for no user benefit.
  - ~~**Shelter Members tab convention.**~~ **Resolved 2026-06-01 (shelter strategy conversation).** Mirrors the Communities pattern. **Tab name: Members** (matches Communities — cross-page consistency over per-entity custom terminology). **Filter pills inside the tab:** *Walkers* (vouched to walk), *Supporters* (followers, sponsors, adoption-curious — anyone interested without the walking commitment), *Team* (renders only when staff are linked via the optional staff-linking path). **Default filter:** All. **Single badge per row** identifying their relationship to the shelter (Walker tier badge, Supporter chip, Team role badge); never multiple. **Sort default:** most-recent activity (the non-scoreboard pattern — recent walkers float up; community in motion). Reflects shelter-entity design decisions from 2026-06-01: ShelterProfile is parallel to UserProfile (not a Group), shelter accounts are institutional-by-default (shared login; optional staff-linking path), no contained group for the walker pool. Lives in [[Cold-Start Playbook]] thread; will move to a `features/shelters.md` doc when the Shelter phase opens.
  - Pattern: make a sweep at any phase close to catch new drift. Not blocking demo; quality-of-prose issue.

---

## 10. Demo & Presentation

**Resolved:** Four journey personas (Tereza, Daniel, Klára, Tomáš) plus Shawn (default) and a synthetic "New User" persona for empty-state previews. Mock data layer built with 20 users, 24 meets, 35 posts, 13 reviews, 10 bookings, 8 conversations, 20 group message threads. Image generation prompts prepared (40 prompts across 7 tiers).

**Resolved:** Persona switching works via React context (`CurrentUserContext`) backed by `localStorage`. Five-persona picker (Tereza / Daniel / Klára / Tomáš / New User); Shawn removed from picker 2026-04-26 but remains in mock-world data. Default is Tereza. Three switcher surfaces — profile-page name dropdown (primary), `/demo` route (canonical entry, shareable URL), `?as=<personaId>` URL param (non-persistent preview). Full spec in `docs/features/demo-mode.md`. Closed by Persona & Demo Mode Wiring (2026-04-26).

**Resolved (Mock World Building, 2026-05-02):** Per-persona highlight reels documented in `docs/features/demo-mode.md` → "Highlight reels." 3–4 surfaces per persona where their story reads strongest, mapped to URLs reviewers can click through.

**Resolved (Demo Narrative & Personas + Guided Walkthrough Build, closed 2026-05-18):** Both questions answered together. The demo is **persona-led with scenario framing** — `/demo` surfaces a threaded four-beat narrative (an anchor event with per-persona POV offshoots), each beat a persona drop-in. And it offers **both modes**: a **Guided Walkthrough** (auto-switching personas through the beats, full-screen interstitials + a persistent on-surface step card) AND **Open View** (free persona-picker exploration). Specs in `docs/features/demo-mode.md`; narrative spine in `docs/strategy/Demo Narrative.md`.

**Refined (Demo Narrative V2, closed 2026-06-01):** The V2 phase re-authored the walkthrough as a **3-beat, 2-persona narrated concept story** (Daniel → Klára → Daniel) framed around the walker-trainer hybrid. Pre-staged content, mid-beat interstitials (time-passage + explainer), fire-off step type, a new private-group "Care from neighbours" surface. `/demo` was folded into the landing page (`/`) — the launcher is the demo's front door. The walkthrough scaffolding moved to dark "system" chrome; pause/resume retired in favour of minimise/restore + a quiet Exit link.

---

## 11. Technical

**Resolved:** Chat uses mock data, designed for Supabase Realtime swap.

**Resolved (Mock World Building A1 / 2026-04-30):** Provider ID dualism — bridge contract formalised. Bridge documented at top of `providers` array in `mockData.ts`: providers may exist as directory-only `ProviderCard` entries (no `UserProfile`) or be linked via `userId` to a real user. `getUserOrProvider` synthesises minimal profiles for directory-only providers. No phantom IDs remain in active code paths.

**Resolved (Mock World Building / 2026-04-30):** Connection state per-persona — `mockConnectionsByViewer` now keyed for Shawn (12), Tereza (8), Daniel (5), Klára (10), Tomáš (6). Tier rendering works correctly across all personas. New User intentionally has zero connections (empty-state preview).

**Open:**
- **Notification strategy + user-controlled settings.** *(Originally: "what triggers push vs. quiet delivery? Belongs to Inbox & Notifications phase.")* Surfaced again 2026-05-08 during the Inbox & Notifications walkthrough. Two intertwined questions: **(a) Delivery channels** — what's push vs in-app vs email vs quiet, and how does that map per notification type? Demo-state we have only the in-app bell; the channel question is a Supabase-backend / production concern. **(b) User settings UI** — a `/settings/notifications` surface where users toggle types on/off, set quiet hours, mute specific bookings/groups. Three implementation levels sketched: (1) UI scaffold only (visible toggles, no behaviour) ~1–2 hrs; (2) working type-level toggles via `usePersistedState`, scoped per persona ~half day; (3) per-context preferences (mute this booking / group / sender) — bigger. Recommended home: **Onboarding & In-Product Communication phase** — fits with the broader "users configuring their experience" thematic (privacy, tier, profile visibility). Don't ship hollow; defer until that phase has the room. The Inbox & Notifications phase shipped the bell delivery + lifecycle-update pattern; settings UI was deliberately out of scope.
- Supabase data model for production — design before building backend.
- Image storage/hosting for galleries and meet photos.
- **Active-session elapsed-time drift.** The "Active now · NN min" elapsed counter shows up in two places (in-page Active panel + sidebar/mobile banner). Each surface runs its own `setInterval(60_000)` against a local `now` state, with `floor((now - checkedInAt) / 60_000)` for the displayed minutes. Two consequences: **(1) drift between surfaces** — the two intervals start at different component-mount moments, so their tick edges don't align. You can see the panel reading 1 min while the sidebar reads 2 min for the same session. **(2) drift from real time** — the floored value is whatever was computed at the *last* tick, so the display can be up to ~60s stale. Combined, in the worst case the panel can read 1 min when real elapsed is 4 min. Surfaced 2026-05-07 during B7 walkthrough — **does not affect product behaviour**, just the displayed counter. Fix sketch: a single `ClockContext` with a faster tick (e.g. 5s), all consumers re-derive against `Date.now()` on each render. Single source of truth, ~5s accuracy ceiling, no proliferation of intervals. Trivial component but rippling change (`ActiveSessionPanel`, `useActiveSession`). Defer until a polish pass — pre-demo cleanup or Demo Presentation phase.

---

## 12. Content, Photos & Tagging

**Assumption:** Every meaningful interaction (a walk, a training session, a park hangout) can produce a photo. Photos are the long-term value of the social side of the platform — they accumulate as relationship evidence over time. The platform's job is to surface them well, gated by the right privacy rules. (Surfaced 2026-05-11 during Profiles Deep Pass walkthrough on B6.)

**Refs:** `features/profiles.md` (Posts tab + Highlights + per-post kebab spec), `features/shelters.md` (shelter-dog Photos + Highlights), `Content Visibility Model.md` (two-gate rules, +Filter pattern), `archive/phases/photos-and-galleries.md` (build record).

**Resolved (Profiles Deep Pass walkthrough, 2026-05-11):** **Per-dog albums are auto-derived from tagged posts; curated `photoGallery` becomes "Highlights."** Subsequent: shipped 2026-06-04 in the Photos & Galleries phase as a unified Posts surface (List ⇄ Grid view toggle, +Filter tag-type pills) on BOTH `/dogs/[id]` and `/profile/[id]`, with a Highlights strip above. `PetProfile.photoGallery` renamed to `PetProfile.highlights`. New `UserProfile.highlights: string[]` field. Tag-pending approval moved to `/notifications`. Tags never expand audience (enforced via `getPostsByDog(dogId, viewerId)` + `isPostVisibleTo`).

**Resolved (2026-06-01) — phase-blockers cleared, shipped 2026-06-04 with refinements:**

- ~~**Owner moderation affordances.**~~ **Shipped, unified onto the per-post kebab.** The three-distributed-surfaces design (per-post 3-dots + long-press + per-dog gear) read as finicky during the build; replaced with a single `PostKebabMenu` covering everything per post: Pin/Unpin to Highlights, Hide/Show in album, Edit post, Delete post, Untag {Dog}, Report, Block. Pin + Hide are universal (anyone can pin from any author's post; Hide gated only by dog-page-ownership context). Edit/Delete author-only; Untag/Report/Block non-author-only. Long-press surface retired. Per-dog gear (Auto-approve / View hidden / Clear pinned) survives as a small settings menu next to the section title. Same control set applies to shelter dogs — `ShelterProfile.tagApproval` is the authority, no operator UI in V1 (V3+).
- ~~**Tag approval semantics for dogs vs owner.**~~ **Shipped.** Tags inherit from `UserProfile.tagApproval`; shelter dogs use `ShelterProfile.tagApproval`. Tag-pending approval surfaces on `/notifications` as `tag_pending` rows (matches Instagram/Twitter/Facebook pattern; the originally-spec'd About-tab queue was refactored mid-phase). Pre-existing tags grandfathered-approved; explicit carve-out via `DEMO_PENDING_TAGS` constant for demo curation. Pending stays pending until owner decides (no auto-approval from inaction). Per-dog override remains V2.
- ~~**Cross-pet households.**~~ **Shipped.** Per-dog highlights + per-dog posts, separate albums for multi-pet households. No aggregate owner-level "all my dogs" primary view.

**Resolved during the Photos & Galleries phase (2026-06-04):**

- ~~**Profile-level user Highlights.**~~ **Shipped.** `UserProfile.highlights: string[]` field parallel to `PetProfile.highlights`. Strip above the Posts collection on `/profile/[id]`. Tappable thumbs open a lightbox in "Highlights mode" (carousel = the highlights themselves, URL-resolved globally so cross-author entries work, within-post nav hidden). Forward direction: when albums-as-tag-type V2 lands, Highlights becomes one album option in a dropdown switcher on the strip header.
- ~~**Photo URLs storage.**~~ **Held.** Highlights still store photo URLs (not `{postId, photoIndex}` pairs) because (a) the URL-to-post resolution at render time is cheap, (b) it generalizes if highlights ever point to non-post photos in the future. Trade-off: orphaned URLs render as non-clickable.
- ~~**Post detail surface.**~~ **Shipped as a modal lightbox, not a route.** `PostLightbox` via `PostDetailContext` — photo-led overlay used by PhotoGrid tiles, in-feed photo taps, Highlights, tag-pending notifications. Comments aren't a primary platform thread, so a `/posts/[id]` route was deliberately not added.

**Open:**
- **Post-publish tag adding.** Two cases, both connection-gated symmetrically. **(1) Author adds tags to their own post after publish** — limited to entities the author is connected to (people, dogs, communities, shelters). Lives in the per-post three-dots menu as "Edit tags" alongside Untag. Not blocking V1; lands when a related phase ships. **(2) Non-author suggests a tag on someone else's post** — requires the suggester to be connected to the post author / community, AND the request flows through the existing `tagApproval` model on the receiving side (inverse direction of the current setting — same approval mechanism). Use case: Tereza sees a Klára-posted photo with Bára visible and wants Bára tagged so it lands in Bára's auto-album. Carer Portfolio abuse-resistance preserved (§8 resolved — portfolios don't aggregate from tags). V2+ territory; logged so it's a conscious deferral.
- **Albums beyond Highlights — albums as a tag type.** V1 model = Highlights (curated `photoGallery: string[]` per dog) + tag-filtered views (auto-album = filter by dog tag). V2 model = tags get a `type` field (`person | dog | location | album | ...`) and "album" is one type. Album creation = creating an album-type tag (scoped to a user / community / shelter). Adding a photo to an album = tagging it with that album-tag. Viewing an album = filtering posts by that tag. Unifies albums with the existing tag mechanism — no separate album CRUD, no separate membership data, one model handles person/dog/location/album. Highlights stays as the V1 mechanism (curated pin-above-the-fold per dog). V2 timing: when albums earn their keep — testers ask for them or a shelter use case (e.g. "Adoption alumni" curated collection) makes it concrete.

---

## 13. Service ↔ Meet Linkage Model

**Assumption:** Services and Meets are **independent entities** that can be **linked**. Neither owns the other; the link itself has properties (required vs optional, one service → N meets). This model resolves the long-running ambiguity around how paid-roster offerings (training sessions, paid group walks, workshops) get authored, managed, and discovered. (Surfaced 2026-05-13 during Profiles Deep Pass walkthrough on C7a — Klára's 4 seeded Meet-type "services" exist nowhere in the edit UI; the footnote claimed they were "managed separately" but no management surface exists.)

**Phase:** Implementation is **Service ↔ Meet Linkage phase** (`docs/phases/service-meet-linkage.md`, draft 2026-05-13). The model below is pre-decided; the build-time open questions at the bottom of this section travel with the phase.

**Refs:** `phases/service-meet-linkage.md`; `Groups & Care Model.md` → Service ↔ Meet linkage; `features/explore-and-care.md`; `features/meets.md`; `Cold-Start Playbook.md` (trainers are the anchor cold-start carer type and most of what they sell is Meet-type).

**Resolved (Profiles Deep Pass discussion, 2026-05-13):**

**The four canonical configurations:**

| Configuration | Example | Service | Meet | Link |
|---|---|---|---|---|
| Free unlinked Meet | Tereza's Sunday park walk | — | exists, free | — |
| Free Meet + linked drop-off Care service | Tereza's free Thursday walk — neighbours **join as walkers** (free RSVP, own dog); owners can *separately* **book a drop-off Care walk** so a carer takes their dog. One path or the other, never both. | Care (drop-off — owner absent) | exists, free | optional — **book ≠ attend** |
| Meet with required service | Klára's Group training session — must book to RSVP | exists, RSVP is the booking | exists, paid-only roster | required (booking = RSVP) |
| Service with no Meet | Boarding, house-sitting (pure Care) | exists | — | — |

**Correction — config #2 remodelled (2026-05-17).** Config #2 was originally "Meet with optional service" — a free walk whose paid linked service put bookers on the *same roster* as free attendees ("mixed roster," "monetizes attendance"). That was wrong, and the Service ↔ Meet Linkage build inherited the error (Tereza's "Group walk" was seeded as a *Meet-type* service; booking it called `setMeetRsvp("going")`, faking attendance). A drop-off "Group walk" is a **Care service** ("I take the dog" — see Groups & Care Model's Services-as-Catalog table + the onboarding routing test), and a paid-walk customer *by definition does not attend* (you pay a walker so you don't have to). Corrected config #2: a **free meet** linking one or more **drop-off Care services**. Two engagement paths — free RSVP (you come, with your dog) vs. booking a linked Care service (a carer walks your dog; you don't come) — and a user does one, never both. **Booking is decoupled from roster attendance:** the booked owner is not a social attendee; only their dog is present, with the carer. The corrected config #2 was **built in the Service ↔ Meet Linkage phase as Workstream H** (2026-05-17): `CarerCareServiceConfig` gained an optional `id` (set when meet-linked); `Meet.linkedServices[]` references a Care service id with `required: false`; the meet detail shows a drop-off-Care callout (`LinkedCareCallout`) separate from the free RSVP; `DropoffBookingSheet` creates a Care `Booking` **without** `setMeetRsvp` (book ≠ attend, verified). `meet-15` re-links Tereza's `walks_checkins` Care service (meet-authoritatively — `meet-15.linkedServices`; the Care config carries no `linkedMeetIds`). **Link authoring is deferred.** A carer-side picker on the Care service card was built then removed — putting a "link a meet" affordance on the Care card made it read as a confusing peer of "Session offering." Resolved: Care↔meet link authoring belongs on a **meet-side surface** (the meet declares which drop-off services run on it), which also handles **multi-carer** linking. It needs a meet-edit screen that doesn't exist (`MeetComposer` is create-only). The phase seeds the link; authoring is a scheduled follow-on. The required-link config (#3 — Klára's training) is unaffected: a paid *session you attend* is real, and booking-as-RSVP is correct there.

**Shipped — Service ↔ Meet Linkage phase closed 2026-05-17.** The phase delivered: the carer authors Care + Meet + Appointment services in one Services-tab edit; one booking flow (`BookSessionSheet`) from both doorways (carer's Services tab + linked meet detail); required-link meets collapse free RSVP; config #2 owner-facing (Workstream H — free meet + drop-off-Care callout + `DropoffBookingSheet`, **book ≠ attend**, the booking carries `Booking.dropoffMeetId`); `klara-1on1` reclassified Meet → Appointment. **Shipped data model:** `CarerMeetServiceConfig.linkedMeetIds: string[]` (carer-side) ↔ `Meet.linkedServices: { serviceId, required }[]` (meet-side, authoritative for the `required` flag) — note the shipped field is `linkedServices`, NOT the `linkedServiceIds[]` sketched below. The walkthrough walked clean. Two slices carry forward as their own follow-ons (see "Still open"): config #2 **link authoring** and Meet-service surfacing in `/discover/care`.

**Refinement — Walk Service Delivery phase closed 2026-05-20.** Config #2's user-facing name retired "drop-off Care booking" in favour of **"linked-care booking"** — the previous name overloaded a single word (drop-off) across the booking-shape axis AND a delivery-method axis. The booking sheet was renamed `DropoffBookingSheet` → `LinkedWalkBookingSheet`; the typed field `Booking.dropoffMeetId` stays. Delivery is now a first-class priced axis on walks_checkins services (`deliveryOptions: { method: "pickup" | "dropoff"; price: number }[]`) — the owner picks at booking time and the choice persists on `Booking.delivery`. The two axes are documented in `Groups & Care Model.md` → "Two axes" and `features/explore-and-care.md` → "Walk Service Delivery additions."

**Service is the "monetization wrapper."** It owns: price, pricing modifiers, sub-services / what's included, notes, enabled toggle, audience (circle vs anyone). For Care-type services it ALSO owns the structural fields (service type, price unit, etc.) since there's no Meet to delegate to. The Meet (when present) owns the operational fields — location, cadence, occurrences, attendee cap, RSVP rules.

**Cardinality is one-to-many.** One service → N possible linked meets. Klára's "Group walk" service is one product (300 Kč/visit, here's what's included, here's the bio about my experience); she can offer it on her Tuesday walk AND her Saturday walk without duplicating the service entry. Data model: `Meet.linkedServiceIds[]` (a meet can advertise multiple services; in practice usually 0–1).

**Book routes through the same flow from both surfaces.** Tap Book on the meet detail page OR on the carer's Services tab → same booking sheet. If the service is linked to multiple meets, the booking sheet shows a session picker (dropdown of upcoming occurrences) so the owner can pick which one. If linked to one meet, defaults to next occurrence. On commit: Booking record created; attendee added to the meet's roster (if linked).

**Meet card UX by link type.** When a meet links an *optional* drop-off Care service (corrected config #2), the meet card's primary CTA stays "Join free" — the meet's identity is a community walk. The linked Care service surfaces inline as a *separate* supplementary callout ("Have a carer walk your dog: 200 Kč →") that routes to the drop-off booking flow, not the RSVP — book and join are distinct paths. When the link is *required* (config #3, no free path), the free CTA collapses and the card shows "Book session → 350 Kč" as the sole CTA.

**Cross-surface discovery (cautious).** A meet with a required service surfaces in BOTH `/discover/meets` (filter "Training") AND `/discover/care` (filter "Training" / appointment type). Same record, two doorways — different filter frames for different user intents. This is a feature, not duplication, but it requires deliberate filter design so the result counts don't double up confusingly.

**Resolved at phase close (2026-05-17):**
- **Booking-sheet session-picker UX** — one unified occurrence list (soonest default-selected), spanning every linked meet's upcoming occurrences, for both 1-link and N-link services. No dropdown/route split.
- **Service delete mid-flight** — soft-archive (`enabled: false` + `softDeletedAt`) when the service has bookings, hard-delete when it has none. "Has bookings" is proxied by the linked meet's roster (`Booking` carries no `serviceId` back-reference). Archived services keep resolving for existing bookings; future viewers see the meet without the link.
- **Edit-on-Meet redirect** — **N/A.** `MeetComposer` is create-only and a `Meet` carries no service-owned editable field (price lives on `CarerMeetServiceConfig.pricePerSession`). Services own price/notes/modifiers; meets own location/cadence/roster; nothing is shared, so nothing needs locking.
- **Where does the phase live** — it was the standalone Service ↔ Meet Linkage phase; opened 2026-05-13, closed 2026-05-17.

**Still open / deferred:**
- **Config #2 link authoring.** A meet-side surface to declare which drop-off Care services run on a meet (also handles multi-carer linking — a host advertising other carers' services). Needs a meet-edit screen; `MeetComposer` is create-only. Scheduled follow-on — see ROADMAP "Config #2 meet-side link authoring."
- **Meet-type service surfacing in `/discover/care` (E1).** `/discover/care` is built around the four-service Care taxonomy (filter pills, `ServiceType`-keyed pricing, sub-service accordions); surfacing Meet-type services (filtered to "Training" → `BookSessionSheet`, not the Care inquiry flow) needs deliberate filter/card design. The optional-link service price chip on `/discover/meets` `CardMeet` defers with it (needs carer-resolution inside the hot list component). Phase-sized — a focused follow-up, not a punch-list fix.
- **Auto-pricing engine extension to Meet-type services.** `computeQuote` stays Care-only; Meet/Appointment keep flat `pricePerSession`. Extending it (last-minute / holiday session pricing) is a follow-on.
- **Per-service draft/publish toggle.** A "Published" toggle on the Meet/Appointment cards was removed mid-phase — its off-state had an unresolved implication for required-link meets (unpublishing the service removes the meet's only booking path). Per-service draft/publish is its own model; design it alongside the pricing-modifier extension. The `enabled` field stays in the data model (soft-archive + view-mode filtering use it).
- **Lifecycle when a meet cancels with attached service-bookings.** Refund / reschedule path for `meetBooking` + `dropoffMeetId` bookings. Not addressed this phase; reuses the existing cancellation flow but the message + refund design is open.
- **Multi-doorway filter-count dedup.** A required-service meet surfacing in both `/discover/meets` and (once E1 lands) `/discover/care` — result counts may double-count the same record. Deferred refinement.
- **Free → paid upsell.** An owner who joined a meet free can't upgrade to a linked service in-context. Deferred; flagged so the booking sheets don't paint into a corner.
- **Privacy semantics on the link.** Meet visibility × service audience. Working assumption held this phase — the service gates booking on the carer's audience; the meet keeps its declared visibility for free attendees — but it wasn't formally pressure-tested. Revisit when config #2 link authoring opens.

---

## 14. Shelters & Institutional Accounts

**Shipped 2026-06-02 (Shelter Foundation phase close).** Útulek Liběň seeded as the demo shelter; `/shelters/[id]` page with Feed/Dogs/Members/Gallery tabs; minimal `/dogs/[id]` profile; volunteer badge system on Members tab; Follow + Walk-a-dog action row. Build delivered against the 2026-06-01 resolutions with two notable evolutions: (a) walker tier naming shifted from "Vetted/Experienced/Trusted Walker" to **"Volunteer / Regular Volunteer / Super Volunteer"** with violet + growth-icon visual system (paw-weight progression and info-blue were too subtle / wrong semantic), (b) icon shape progression (Leaf → Plant → Tree) replaced weight variation as the tier signal. See `features/shelters.md` for the shipped state, and FC9 + FC11 for the deferred work (cross-shelter aggregation, tier visual intensification, violet→tokens promotion).

**Shipped 2026-06-08 (Help a Dog Discover door phase close).** Fourth Ways In door wired at `/discover/help-a-dog`. Dogs/Shelters pill toggle on a single surface (Dogs default, photo-led, "Needs walks now" sort; Shelters lists the three rescues). Two thin shelters seeded for density — **Pes v nouzi** (Holešovice, 5 dogs) and **Druhá šance** (Karlín, 4 dogs) — each with empty walker + supporter rosters, one shelter-authored post, and the same `/shelters/[id]` chrome handled by thin-shelter empty-state adjustments. Source-aware back from `/dogs/[id]` now prefers `lastListPath` when the source was a `/discover/` route, falling back to the shelter Dogs tab. In-circle elevation deliberately skipped — walkers don't bridge to UserProfile yet; that hook lights up with the credentialing-moat walker journey. See `features/shelters.md` → "Discovery" for the shipped state.

**Assumption:** Doggo will support institutional accounts (starting with shelters) as a parallel entity type to `UserProfile`. Shelter accounts have shared-credential ("the shelter's account") access by default, with optional individual-staff linking ("Team"). Walkers are individual users vouched per-shelter with tiered permissions. Dogs are non-owned `PetProfile` records (containment in `ShelterProfile.dogs[]`, no `shelterId` discriminator field). Shelter page surfaces mirror the Communities tab pattern (Feed / Dogs / Members / Gallery). See [[Cold-Start Playbook]] → "The shelter angle" + the 2026-06-01 strategy conversation.

**Refs:** `Cold-Start Playbook.md` (shelter angle, dog-as-unit framing, walker tiering), `Trust & Connection Model.md` (shelter walker credentials — forward cross-reference), `features/shelters.md` (shipped state).

**Resolved (2026-06-01 strategy conversation):**

- **Entity shape.** Shelter is a top-level entity (`ShelterProfile`) parallel to `UserProfile` — NOT a fifth Group type. No contained group for the walker pool. Considered four options (group-type, top-level org, UserProfile-with-flag, hybrid org-containing-group); chose top-level org (Option B) for its alignment with how shelters actually operate (institutional identity, non-owned dogs, vetted walker pool, per-shelter policy, dog roster, multi-staff admin — six features that don't fit Group cleanly).
- **Account model.** Institutional-by-default: shared login, shelter logo as avatar, posts attributed to the shelter. Eliminates onboarding friction (no per-staff account setup; survives staff turnover). **Optional staff-linking ("Team")** path for shelters that want individual presence — invitation flow from shared account to individual UserProfiles; Members tab gains a Team subsection; "Run by" line on the shelter info card scales from generic ("Run by the Útulek Liběň team") to avatar-stack. Same page shape with or without linked staff.
- **Tab structure.** **Feed / Dogs / Members / Gallery** — mirrors the Communities pattern with one substitution (Meets slot → Dogs, because shelters don't host meets as a core feature). Feed = shelter info card + dogs-in-care summary card + post stream (interleaving shelter-authored celebrations + walker-authored walk photos; posts about specific dogs deep-link to dog profile). Dogs = roster surface, default sort "Needs walks now," single-column photo-led card feed. Members = filter pills (Walkers / Supporters / Team), single-badge-per-row, sort by recency. Gallery inherits the Photos & Galleries phase machinery.
- **Walker tier model.** Three per-shelter institutional tiers — **Vetted Walker** (solo walks only, default after vouching) / **Experienced Walker** (group walks with vetted-sociable dogs, ~10 walks at this shelter) / **Trusted Handler** (group walks with reactive or unknown dogs, ~25 walks + coordinator sign-off). **Per-shelter policy** gates group walks regardless of walker tier (some shelters never permit group walks — first-class case). **Per-dog overrides** exist independently (`soloOnly`, `experiencedHandlersOnly`) and union with shelter policy at the walk-eligibility check (strictest rule wins).
- **Credentialing-moat phase merger.** Shelter walker tier badges and Carer Portfolio aggregate badge share the same visual escalation language (outlined → filled → filled+ring) and TrustBadgeStrip placement → **merged into one credentialing-moat phase** when it opens (per §7 + §8 resolutions). Different taxonomies (per-shelter named tiers vs. cross-engagement single badge), shared infrastructure.
- **Anti-scoreboard discipline.** No leaderboards, no streaks, no "top walker this month" ranking. Visible accumulation through badge intensification + absolute stats (per-profile, not ranked) is the recognition pattern. Sort defaults favour recency over ranking (Members tab; Dogs tab "Needs walks now" surfaces urgency not competition).

**Open:**

- **ShelterProfile → OrgProfile generalization.** When the second institutional account type lands (rescue org, vet clinic with public face, training school, breed-specific org), do we generalize `ShelterProfile` to `OrgProfile` with `kind: "shelter" | "rescue" | "vet" | ...` and shared infrastructure? Working assumption: yes, generalization makes sense — most distinguishing features (institutional identity, member roster with badges, non-owned dogs, policy) apply to other org types too. But premature until the second type exists. Decide when shelter implementation is stable and a second org type is queued.

- **Adopted-dog transition pattern.** When a shelter dog gets adopted, what happens to her profile? Strong direction (2026-06-01): she becomes a celebration card briefly (pinned at the top of the shelter's Dogs tab for ~a week), then transitions to an archived state in a "Happy endings" subsection in About. Walkers who walked her keep her in their personal dog gallery as a completed-relationship marker. **Open:** does the profile *transition* to a `PetProfile` owned by her new family (highest-narrative-value, requires linking the adopter's UserProfile — possibly with the adopter's consent flow), or does the adopted version stay shelter-owned in archived state? Transition version is very on-brand and worth the design work for V2; archive-only is V1-acceptable.

- **Vouching state machine.** The walker vouching process has its own state machine: *applied → invited to visit → vouched → vetted walker*. UX for each transition: how does the walker apply (form on the shelter's Get Involved surface? CTA from a dog profile?), how does the shelter review and respond (shared-account inbox? coordinator-role staff member?), what notifications fire on each transition. Demo-state can fake the happy path with a brief delay; real implementation is a meaningful flow.

- **Per-shelter policy authoring + storage.** Per-shelter policies (group walks permitted: off / on / by-tier; minimum walker tier for any walk; tier-advancement rules: auto-after-N-walks vs coordinator-approved-only; bilingual posting language; vouching process notes) need a data shape on `ShelterProfile` and an authoring UI on the shelter account settings. **Data shape shipped 2026-06-02** (`ShelterPolicy` interface on `ShelterProfile`); authoring UI deferred to the shelter operator surface (V3+).

- **Per-dog policy overrides UX.** `soloOnly`, `experiencedHandlersOnly`, and similar per-dog flags need an authoring affordance on the shelter's dog-edit surface. Union with shelter-level policy at the walk-eligibility check (strictest rule wins). **Data fields shipped 2026-06-02** (rendered on dog profile as a policy strip); authoring UI deferred to the shelter operator surface (V3+).

- **Optional Team mode UX design.** When a shelter opts into linking individual staff: invite-by-handle-or-email from the shared account; staff member accepts from their own UserProfile; staff member gains "Staff at {shelter}" role badge on their own profile + visible in the shelter's Members > Team filter; "Run by" line on the shelter info card scales to avatar-stack with linked staff. **The key UX discipline:** the page should look right with zero linked staff or with five linked staff — same shape, scaled fill-in, not two different page modes. Design when basic shelter surfaces are stable.

- **Shelter operator (admin) view.** Most of the shelter operator's surface (dashboard of dogs needing walks, walker applications pending queue, recent visit reports to skim, posts to publish, dog-edit affordances) is V3+ territory. Demo probably stubs admin actions with state-toggle buttons (approved/denied buttons that just toggle state, no real permission gating); full operator UX waits for real shelter conversations. Worth being honest about: building the operator side properly is a meaningful chunk of work post-demo.

- **Cross-shelter walker affiliations on user profile.** Profile "Volunteer work" section: someone vouched at 2+ shelters with different tiers. Visual hierarchy (highest tier dominant; others compact); how multiple "Vetted Walker at {shelter}" entries render together; whether the section header carries an aggregate ("Vetted at 3 shelters · 47 walks") or stays per-shelter only. Designed alongside the Carer Portfolio + Shelter Walker Credentialing merged phase.

- **Shelter "Events" escape valve.** For the rare case shelters want to host something (open day, adoption fair, volunteer training). NOT a Meet — meets carry community machinery (Familiar marking, post-meet review) shelters shouldn't operate. Sketched as a small `ShelterEvent` shape (org-hosted, RSVP'd, no Familiar-marking, no post-meet review). Design when first real shelter conversation surfaces a need; possibly never needed.

- **Long-stayer treatment.** Dogs in the kennel 30+ days. **Shipped 2026-06-02:** auto-derived white-glass chip overlay on the Dogs-tab card, "Longest in care" sort option in the sort menu, dog-profile chip + subline ("Long-stayer"). **Still open:** distinct card treatment (heavier card border, urgency framing copy like "Looking for a special home"), profile-level long-stayer treatment beyond the chip, push-notification or feed-prominence for very-long-stayers. High-impact small surface for the dogs who need it most.

- **Incident reporting from visit reports.** Visit reports support flagging in principle; the institutional side (does the shelter see the flag, does it auto-impact walker tier, does it require coordinator review, what happens to the dog's record, does the dog's per-dog policy auto-tighten in response to repeated incidents) is real design work. Sized small for V2; demo can show that the flag exists without the full workflow. **Adjacent:** §5 Safety & Liability "trust regression" question applies here — incident-driven tier demotion is a real instance of trust regression we'd need to handle.

- **Bilingual surfaces (Czech/expat).** Útulek Liběň's coordination is in Czech; the expat dog community is English-first. A bilingual coordination layer (shelter posts in both languages, dog-profile content in both, vouching process notes in both) is unusually valuable for shelter onboarding specifically. Probably out of V2 scope but worth logging so we don't bake English-only assumptions into shelter post-authoring surfaces.

- **Walker-circle elevation on Discover Help a Dog** *(opened 2026-06-08 with the Discover door close)*. The Discover Care surface elevates carers the viewer has connection to (`Carers in your circle` / `Other carers`). Help a Dog ships flat — walkers don't bridge to `UserProfile` yet, so there's no honest circle relation to elevate. When the walker journey ships (credentialing-moat phase), this lights up: "Dogs you've walked" and "Shelters your circle volunteers at" become the elevation hooks, sharing the same `ResultsSectionHeader` `in-circle` variant. Worth deciding ahead: is the elevation row-level (each dog/shelter card carries a "Walked by N people in your circle" line) or section-level (split the feed into two groups)? Section-level matches Discover Care; row-level might be lighter given how thin the shelter-dog social graph will be even after the walker journey.

- **Thin-shelter content-authoring authority** *(opened 2026-06-08)*. Pes v nouzi and Druhá šance shipped with empty walker + supporter rosters by design — the walker journey doesn't exist yet, and bridging directory-style walkers per-shelter would multiply the seeding burden. **Open:** when the walker journey ships, do the two thin shelters get walker rosters seeded, or do they stay as "small operations, no walker pool"? The latter is more honest demographically (small rescues genuinely run on the founder + a handful of volunteers, sometimes zero credentialed walkers); the former carries more demo richness. Either lands cleanly — pick when scoping the walker journey.

---

## 15. Vaccinations — disclosure, verification, gating

Added 2026-06-02 from PO user interviews (Roman). Today `VetInfo.vaccinationsUpToDate` is a single boolean. Roman wants structured per-vaccine records (Rabies / Parvovirus / Distemper / Hepatitis / Parainfluenza), an explicit acknowledgement-of-accuracy framing, and gating of meets so unvaccinated dogs can't join.

**Resolved (V1 — Dog Profile phase, 2026-06-03):** Shipped owner-self-declared structured records. Field shape: `VaccinationRecord { type, lastGivenAt, confidence: "self-declared" }` array on `VetInfo` + single per-dog `vaccinationsAcknowledgedAt` (not per-vaccine — acknowledgement is "I confirm THIS DOG's record is accurate as of today"). Rabies treated uniformly with the other four (no platform legal hierarchy; the 5 standard CZ types render in canonical order — Rabies first, then Parvovirus / Distemper / Hepatitis / Parainfluenza). Display surfaces: Syringe-prefixed chips on `/dogs/[id]` (Health section) + PetCard Health & vet section (chips). Edit: PetEditCard Health & vet — per-vaccine date input row (5 rows always shown) + single confirmation checkbox. No gating, no verification — informational only. Helpers in `lib/petUtils.ts:isVaccinationsCurrent / sortVaccinations / formatVaccinationDate`. Vocabulary + ordering in `lib/petUtils.ts:VACCINATION_LABELS / VACCINATION_ORDER`. Seeded on 9 owned dogs + 4 shelter dogs (varied state: full records, older records, mid-puppy-series, refreshed at intake).

**Still open (V2 — gating + verification, deferred):**
- Without verification, gating is theatre. Do we ship gating-on-self-declared (social pressure / liability transfer) or wait until verification exists?
- What verification path? Photo of booklet (moderation) or vet-confirmed via [[strategy/Vets as a Credentialing Layer]] (full institutional)?
- Who sets the gate — meet host, group admin, platform default?

**Forward direction:** the verification path resolves via §16 (Vets as credentialing layer). V2 waits on that decision.

**Refs:** [[strategy/Vets as a Credentialing Layer]] · [[meetings/po-briefing-2026-06-02]] · [[features/profiles]] → Vaccines V1 · `archive/phases/dog-profile.md`

---

## 16. Vets as a credentialing layer

Added 2026-06-02 from PO user interviews. Vaccines without verification are theatre. Verification requires vets in the system; vets bring a multi-sided value loop (owners get peace of mind, vets get patient acquisition, platform gets a verified data layer, public good: more dogs vaccinated). This is the credentialing-as-deliberate-moat thesis applied to a new vertical, with the same shape as shelters.

**Open:**
- Is the thread real? Trigger to graduate from research to phase: PO has a Prague vet conversation (mirrors how shelters graduated 2026-06-01).
- If we proceed, which Tier? Tier 1 (owner-self-declared) is on deck in Dog Profile. Tier 2 (photo-confirmed) is a separate commitment. Tier 3 (`VetProfile` parallel to `ShelterProfile`) is Shelter-Foundation-sized.
- Entity model — A (`VetProfile` parallel to `ShelterProfile`), B (thin record-only entity), C (no entity, photo-only), D (start with C, graduate to A)?
- "Vet-as-Carer is retired" (§6) — maintained? Suggested yes; vets stay credentialing layer, not Care category.
- Cold-start partnerships strategy — same playbook structure as shelters, separate investment commitment?

**Resolved (2026-06-02):** the strategic framing is the research doc at [[strategy/Vets as a Credentialing Layer]] — the multi-sided value loop, entity options, tier breakdown, decisions for PO. The doc is for PO presentation; this OQ entry exists to track whether/when the thread graduates.

**Refs:** [[strategy/Vets as a Credentialing Layer]] · [[meetings/po-briefing-2026-06-02]] · [[strategy/Cold-Start Playbook]] (shelter precedent)

---

## 17. Appointment meeting-options model

Added 2026-06-02 from PO user interviews. The current `AppointmentBookingSheet` surfaces no location to the owner — Klára's 1-on-1 training booking flow says nothing about where the session happens (mobile vs. facility) or whether the trainer picks up the dog. Roman flagged the gap.

**Open:**
- Q1 — generalise into a shared "meeting-options" abstraction across walks_checkins + appointments (extends/replaces `deliveryOptions`), or keep them separate (`appointmentLocations` on `CarerAppointmentServiceConfig`)?
- Q2 — curated tuples (~4 named patterns: "Carer comes to you" / "You bring dog to carer" / "Carer picks up + meets at a public place" / "Owner + carer meet at a public place") or carer-defined free-form (label + price)?
- Q3 — default-recommended option per service type? Training → "Carer comes to you" (matches Prague mobile-modal). Facility-based → "You bring dog to carer."
- Q4 — pricing engine impact — `computeAppointmentQuote` resolves option-aware base rate?

**Forward direction:** scoped into the **Service Options & Booking Clarity** phase (`phases/service-options-and-booking-clarity.md`) as Workstream B with the Qs as Pre-build scope calls. The phase board sketches recommended answers but holds them for confirmation before opening.

**Refs:** [[phases/service-options-and-booking-clarity]] · [[meetings/po-briefing-2026-06-02]] · [[features/explore-and-care]] (existing `deliveryOptions` precedent on walks)

---

## 18. Should training graduate to a 5th Care service type?

Added 2026-06-02 from PO user interviews. Roman flagged training as the most-needed service for first-time owners + expats, with 11+ training sub-types each with their own price. Today training is split across `Meet` (group training sessions) and `Appointment` (in-house solo training). Three paths:

- A — Promote Training to a 5th Care service type alongside Walks / Sitting / Day-care / Boarding. **Reopens** the four-service Care taxonomy we just stabilised (2026-05-11). High blast radius.
- B — Extend the existing Appointment training variant with structured sub-types (obedience / behaviour / agility / etc. — see PO briefing for the full list); add a Training filter pill on Discover.
- C — A dedicated Training surface on Discover that aggregates training meets + training appointments without re-jiggering the taxonomy.

**Open:**
- Do user-testing signals confirm that training is hard to find / hard to compare via the current taxonomy?
- If yes, does B suffice or does A become necessary?
- C is the lightest move — should we ship it preemptively as a discovery improvement?

**Forward direction:** B's two side tasks (Discover Training filter pill + Appointment `trainingType` enum) ship in this session as punch-list. A is the deferred decision waiting for a user-testing signal. **Do not reopen the Care taxonomy without a real user-testing-confirmed pain.**

**Refs:** [[meetings/po-briefing-2026-06-02]] · [[features/explore-and-care]] (Care Catalog Taxonomy 2026-05-11) · `lib/constants/services.ts`
