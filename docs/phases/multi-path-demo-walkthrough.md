---
status: active
last-reviewed: 2026-06-22
review-trigger: "Update as items are walked, edit as scope adjusts"
---

# Multi-Path Demo — Three Community Worlds — Walkthrough

Verification checklist for the Multi-Path Demo phase. **All workstreams are built** (A1 registry, B1 launcher doors, C W1 new-owner, D W2 trainer, E W3 shelter, A2 pre-staging, A3 narrative doc, F cross-path framing + probe checkpoints). What remains is the collaborative drive-through review — the deeper shelter beats (modals, state gates) and the probe copy most need a human at the keyboard.

**How to use:** dev server on port 3000 → unlock (`prague2026`) → start from `/`. Each guided path is launched from its own door; Open View personas via the cast cards or `?as=<id>`.

**Available personas:** Daniel (anxious new owner, locked), Klára (walker-trainer), Tomáš (Karlín professional / shelter mentee), Eliška (adoption-curious), Magda (neighbour), Tereza, Lena, New User.

**Seed note:** the walkthrough world is shared across paths (one Stromovka walk, one Klára, one Daniel). The paths are different POVs/subsets over that one world, so overlap between them is intentional.

---

## Open for your call

- [ ] **O1. The `trainer` path is a single beat; the meet→booking conversion is deferred.** W2 ships as Klára's morning (run the linked-care session → seal the visit report → share the lead-gen post). The trainer thesis's punchline — a client *converts* from the walk — is carried only by the closing copy ("tomorrow's nervous-dog owner books her"), not played from Klára's side. Showing it live needs a pre-staged incoming inquiry for Klára to answer (folds into A2). Worth deciding: is the closing-line enough for a first pass, or is the conversion beat load-bearing enough to build now? (Start the **Build community, and a living** door at `/`.)
- [ ] **O2. W1 was re-cut to Daniel-only; Klára's operational beat was removed, not kept.** The shipped V2 spine (Daniel → Klára → Daniel) became two consecutive Daniel beats; Klára's session-running beat moved wholesale to W2. The "training is the on-ramp to belonging" explainer that lived in that beat was **not** re-added to W1 — the motivation is carried by the existing booking-step framing ("a group walk is a start, but Bára needs focused one-on-one help"). Could re-add a short explainer if payoff #7 feels thin from the owner's side. (Start the **Find your community** door.)
- [ ] **O3. Same-persona consecutive beats now render as a time-passage, not a persona handoff.** W1's Beat 2 is Daniel again (after the walk), so the interstitial shows "Later that day." with no avatar re-intro / "You're now Daniel" / archetype line — instead of repeating the handoff. New interstitial behaviour (`isContinuation`). Confirm it reads as intended. (Drive **Find your community** to the Beat-1→Beat-2 transition, or set `beatIndex:1`.)
- [ ] **O4. Door labels + blurbs.** "Find your community" / "Build community, and a living" as the two door titles, each with a one-line blurb. Owner-facing copy ratification — these are the first thing every interviewee reads. (`/`.)
- [ ] **O5. In-demo assumption-probe checkpoints are built (new `probe` interstitial mode).** The "demo IS the interview" probes now render as distinct full-screen checkpoints (a "For the room" eyebrow + a teal left accent bar) posing the question to the viewer/coordinator. Placed: **shelter** ×2 (Beat 2 — "Would your shelter accept this vouch?" = A1/A8; Beat 5 — "How many of your adopters walked first?" = A9), **trainer** ×1 (Beat 1 — "Would you run a walk like this?" = A5). **For your call:** (a) the wording of each probe; (b) whether `new-owner` should carry a probe too (it tested positive already, so I left it probe-free); (c) whether more of the mentor-script's A# set should be surfaced. (Drive any path to its probe; or see the screenshot in chat.)
- [ ] **O6. W3 was folded onto Eliška's single spine; Tomáš's separate mentee arc was not wrapped.** The board scoped W3 as "Tomáš mentee + Eliška." But Eliška's arc already contains the mentor→vouched mechanism (Beats 2–3), so wrapping Tomáš too would be the same mechanism with a second persona. W3 is single-POV (Eliška); Tomáš stays in Open View (`?as=tomas`) and feeds the Phase-2 operator/interview material. Reversible if you want a distinct trainer-side-of-mentoring beat. (Start the **Help a shelter dog** door.)
- [ ] **O7. W3's vouch is now pre-staged (A2 done); the adoption capstone stays tester-driven.** Beat 3's "Three good walks in" interstitial fires the mentor-vouch on Continue (no more "Complete mentor session (demo)" grind — verified it unlocks "Walk Nora"). The mechanism is still *shown* (the booking hero + the "From walker to trusted walker" explainer). Beat 6's adoption (interested → pending → adopted) is left to the tester via the AdoptInquirySheet's in-context demo controls **on purpose** — it's the climactic hero action, and the sheet's buttons read as the adoption process, not an admin toggle. Confirm you're happy keeping the adoption tester-driven rather than auto-firing it too.
- [ ] **O9. Landing rebuild — specifics to ratify.** Wide, **left-aligned** single-viewport launcher: large headline, the three guided walkthroughs as a row of wider cards, a secondary "Look around freely" bar (strong solid border), footer at the bottom — fits desktop with no scroll, intentional whitespace on the right (no filler — PO call). Free-explore enters as Tereza; copy says "switch characters anytime from the profile page." One flag: the cast cards were removed entirely, so a first-time viewer doesn't meet the personas until they enter (the interstitials introduce them) — say if you'd want a lightweight cast strip back. (See `/`.)
- [ ] **O8. Beat 4's "Share a moment" drives the real composer, not a card fire-off.** Unlike W2's fire-off post, the shelter recap step asks the tester to tap "Share a moment" and post through the real `PostComposer` (pre-tagged Nora + Útulek). A seeded recap (`post-walker-eliska-nora`, with Kateřina's comment) already exists for Beat 5. Worth deciding in review: keep the live composer (more "real"), or switch to a robust card fire-off like W2 (less fragile). (Drive **Help a shelter dog** to Beat 4.)

---

## Worth verifying

> **Self-verified 2026-06-22 (author drive-through).** Confirmed in-browser: 3 launcher doors + correct persona/beat-count per door; W1 continuation interstitial; W2 anchor (Toby startable on Klára's schedule); W3 Beat-1 nav, the "Walk with a mentor" sheet option for non-vouched Eliška, A2 vouch-fire → "Walk Nora" unlocks, the `?finished=1` "Share a moment" banner, Nora's recap + Kateřina's seeded comment in `/home`, and the adopt sheet (no-obligation ladder + demo controls incl. the network-adopts ending); all three probe checkpoints render with the accent treatment. **Bugs fixed during verification:** Beat 3 copy ("Walk with a mentor" lives in the sheet after tapping "Walk Nora", not as a page button); Beats 3–4 modal/runtime-booking steps converted from `awaitAction` nav (dynamic ids would dead-end) to robust manual-advance action steps; Beat 5 now directs tapping the comment; Beat 6 uses the real control labels + the network-adopts ending. Remaining items below are for the PO's eyes (flow/surface sign-off), not mechanical re-checks.

### V1 — launcher + registry

- [ ] **V1.1 The launcher shows one door per registered walkthrough.** `/` → *Expect:* a "Guided walkthroughs" group with two doors ("Find your community", "Build community, and a living"), each title + blurb + arrow.
- [ ] **V1.2 Each door launches its own walkthrough.** `/` → tap each door. *Expect:* "Find your community" opens on Daniel ("You're now Daniel · Beat 1 of 2"); "Build community, and a living" opens on Klára ("You're now Klára · Beat 1 of 1").

### V2 — W1 new owner (Daniel)

- [ ] **V2.1 W1 runs end-to-end as a single-POV owner story.** Start **Find your community** → join the Stromovka walk → (continuation) → mark Magda Familiar → book Klára's 1-on-1 → accept Magda's request → join Holešovice Dog Block → see Veronika's care. *Expect:* coherent Daniel-only journey; no Klára-POV beat.
- [ ] **V2.2 The Beat-1→Beat-2 transition reads as a time-passage.** *Expect:* "Later that day." interstitial (per O3), not a second "You're now Daniel."
- [ ] **V2.3 W1 closing pulls registry copy.** Finish W1. *Expect:* "End of walkthrough." + the "community came first; the care followed" recap.

### V3 — W2 trainer (Klára)

- [ ] **V3.1 W2 runs end-to-end as the trainer's morning.** Start **Build community, and a living** → Start session on Toby → Finish + seal report → Groups → share the walk post. *Expect:* the proven session→report→post flow, now its own path.
- [ ] **V3.2 W2 closing carries the conversion as the payoff line.** Finish W2. *Expect:* the closing names the sealed report + the post that books tomorrow's client (the deferred-conversion stand-in per O1).

### V4 — W3 shelter (Eliška) — the full 5-beat drive-through (FC18-centered)

This path runs through several modals + the group-walk sign-up, so it most needs a human at the keyboard. Drive it from the **Help a shelter dog** door. Note: Beats 2–3 are **action steps** (sign up, then start/finish/share) — if the tester skips the Beat-2 sign-up, Beat-3 has no booking to open, so the sign-up is load-bearing.

- [ ] **V4.1 Door + handoff.** `/` → tap "Help a shelter dog". *Expect:* "You're now Eliška · Beat 1 of 5", lands on the **`/discover` hub**.
- [ ] **V4.2 Beat 1: Discover → Help a Dog → Nora → reach Klára (3 steps).** *Expect:* door → `/discover/help-a-dog` → open Nora → `/dogs/shelter-dog-nora`; tap **Walk Nora** → **Walk with a mentor** → pick **Klára** (her row shows a green "Runs group walks" line). The **mentored-walk sheet** opens. *Discovery bridge: you reach the mentor (and her group walk) through the trainer, not a shelter event listing.*
- [ ] **V4.3 Beat 2: the mentored walk — waivers, then the group walk, all in-sheet (FC18).** In the sheet: **Step 1** agree to both waivers (the shared progress tracker frames both paths) → **Continue** → **Step 2** pick **Klára's group walk** (Nora pre-set; "Or" a private 1-on-1) → **Sign up for the walk**. *Expect:* completes in the sheet (no bounce to the meet page); a meet-linked shelter-walk booking (`subService: Mentored first walk`, free/volunteer) on `/bookings?tab=volunteering`; mentorship begun; "View the walk" link is optional. The open sheet survives the Beat 1→2 handoff. (Verified end-to-end via the live walkthrough.)
- [ ] **V4.4 Beat 3: the walk + recap.** Open the mentored Nora booking on Volunteering → **Start** → **Finish** → `/dogs/shelter-dog-nora?finished=1` → **Share a moment** (pre-tagged Nora + Útulek). *Expect:* the un-vouched mentored booking is startable/finishable; recap posts. **Confirm in review:** can an un-vouched walker start/finish the mentored booking.
- [ ] **V4.5 Beat 3: the vouch fires.** "A few walks later" interstitial → Continue. *Expect:* `fireWalkerVouch` silently makes Eliška a trusted walker (credentialing payoff; A2 — verified standalone).
- [ ] **V4.6 Beat 4: recap reaches the feed.** `/home` as Eliška. *Expect:* recaps of Nora present (followed-shelter gate) incl. Jakub's with Kateřina's interested comment; the "yours among them" copy reads right.
- [ ] **V4.7 Beat 5: adoption to "Adopted".** Adopt Nora → demo controls (meet-and-greet → finalise, network adopts). *Expect:* Nora flips to "Adopted" + happy-ending; closing screen fires.

### V5 — probe checkpoints (the interview mechanism)

- [ ] **V5.1 Shelter credentialing probe.** Drive `shelter` to Beat 2. *Expect:* a "For the room" probe ("Would your shelter accept this vouch?") with the teal left accent bar, distinct from the explainer before it.
- [ ] **V5.2 Shelter advocacy probe.** `shelter` Beat 4. *Expect:* "How many of your adopters walked first?" probe after the advocacy-loop explainer.
- [ ] **V5.3 Trainer supply-side probe.** `trainer` Beat 1. *Expect:* "Would you run a walk like this?" probe (A5) after the "growing business" explainer.

---

## Decisions surfaced during walkthrough

- **Walkthroughs are a named registry, not a single beat list.** `lib/walkthroughBeats.ts` exports `WALKTHROUGH_LIST` of `{ id, displayName, blurb, thesis, interviewee, closing, beats }`; `WalkthroughContext` runs one by id (`start(id)`, `walkthroughId` in run state). Closing copy is per-walkthrough (registry `closing`), no longer hardcoded in the component. → `features/demo-mode.md`
- **The launcher is multi-door.** `/` renders one door per `WALKTHROUGH_LIST` entry (title + blurb), replacing the single "Start the walkthrough" CTA. → `features/demo-mode.md`
- **Same-persona consecutive beats render as a time-passage handoff.** New `isContinuation` behaviour in `WalkthroughInterstitial` (heading = the beat's `when`, no "You're now X" re-intro). Enables single-POV multi-beat paths. → `features/demo-mode.md`
- **The V2 spine split by POV into W1 (new-owner, Daniel) + W2 (trainer, Klára).** `neighbour-care` absorbed into W1; W2 is the standalone Klára morning. The three-World mapping + per-path beats need to land in the narrative doc (A3). → `strategy/Demo Narrative.md`
- **W2 conversion deferred (single-beat trainer path).** Conversion-from-Klára's-side needs a pre-staged inquiry (A2); carried by closing copy for now. → no feature-doc update needed (tracked as O1 + board D1)
- **W3 `shelter` is a single Eliška spine (6 beats) weaving mentor→vouched into the advocacy→adoption story.** Tomáš's separate mentee arc was NOT wrapped (redundant mechanism); it stays Open View + Phase-2 material. The three community Worlds are now all in the registry. → `strategy/Demo Narrative.md` (record the `shelter` arc as a guided walkthrough; reconcile with the Adoption-Curious Arc source section) + `features/demo-mode.md` (registry now ships 3 walkthroughs)
- **A2 pre-staging shipped as step-fired state transitions.** New `fireWalkerVouch` field on time-passage interstitials; `WalkthroughInterstitial` calls a new idempotent `vouchViaMentor` on `WalkerApplicationsContext`. The `shelter` arc uses it to graduate Eliška to vouched at Beat 3's interstitial (no demo-toggle grind). W1/W2 need no mid-arc staging. → `features/demo-mode.md` (document the step-fired pre-staging mechanism alongside the registry)
- **Landing page rebuilt (PO direction): wide, left-aligned, single-viewport launcher.** Dropped the two-half cast-card showcase. Layout: a **left-aligned** hero (large headline with a forced two-line break on desktop) in a wide (1320px) container with intentional right-side whitespace on big screens, then the three guided walkthroughs as a **row of cards** under "Guided walkthroughs", then a secondary "Look around freely" bar (strong solid border) under "Or explore on your own", footer pinned at bottom. Fits a desktop viewport with no scroll; cards stack under 760px. Free-explore drops into the app as the default persona (Tereza); persona switching lives on the profile name dropdown (no per-persona cards on the launcher). PO call on the right-side space: leave it as intentional whitespace (no filler decoration). Removed dead cast CSS/JS; kept the `.demo-picker`/`.demo-pill` block (DemoPersonaDropdown). → `features/landing-page.md` + `features/demo-mode.md` (Switcher surfaces §2 — the landing description is now stale)
- **Booking session header → compact on BOTH the Sessions tab and the active/live page (PO direction).** Added a `compact` variant to `SessionsPetHeader` (avatar-left 140px rounded square per Rule B + name/meta right, gap-xl) and switched both `/bookings/[id]` (Sessions tab) and `/bookings/[id]/active` to it, so hitting **Start session** keeps the header stable and only the body content changes. Also lifts the live-page actions (Log GPS / Add a note / Finish) above the fold and avoids stretching the landscape photo into a band; the empty photo-tile shortened 320→200. **Revises the "pet-as-protagonist on the booking Sessions tab" convention** — the full hero is retired from the tab; the `full` variant is retained but now unused (remove at close if compact-everywhere sticks). → `features/explore-and-care.md` (booking session surfaces) + `implementation/design-system.md` (SessionsPetHeader variants) + **CLAUDE.md** (pet-as-protagonist visual convention — revise) + remove-unused-`full`-variant cleanup at close
- **Mobile nav-header bug on `/discover/help-a-dog` fixed.** `getPageTitle()` in `AppNav.tsx` was missing the route, so on mobile the page showed the Doggo logo with no back/title (the desktop `abovePanel` DetailHeader is CSS-hidden on mobile; the mobile back comes from AppNav's page-title block, which only renders when `getPageTitle` returns a title). Added `/discover/help-a-dog → "Help a Dog"`; mobile now shows "‹ Help a Dog" like the other Discover subpages. → no feature-doc update needed (nav bug fix).
- **Shelter Beat 1 now starts on the `/discover` hub (PO direction).** Was teleporting straight to `/discover/help-a-dog`, which orphaned the back-nav; now opens on Discover with a step to tap the **Help a Dog** door, so the Ways In give context and back-to-Discover is coherent. Beat 1 is now 4 steps (Discover → Help a Dog → Nora → read). → no feature-doc update needed (walkthrough beat copy).
- **Back-button routing fixed (off-by-one in `WalkthroughContext.prev()`).** Back now routes to the surface a step is *displayed on* (the most-recent `advanceOn` strictly before it), not the step's own forward destination — previously Back often left you on the page you were already on, so it looked like Back didn't move. Applies to all paths, within-beat + cross-beat. Modal-only surfaces land on the correct page (can't re-open the sheet). → no feature-doc update needed (infra bug fix; `features/demo-mode.md` step-card spec already says Back routes to the step's surface)
- **Walkthrough card-copy tightening pass + guideline captured (PO direction).** Swept all three paths' card copy: `instruction` = action + arc, `detail` = optional why/how (dropped where redundant), both concise + jargon-free. Removed internal vocab ("doors"/"three doors", "on-ramp", "advocacy loop"/"the loop", "funnel") and over-warm filler ("gentle", "warmest") from user-facing copy; reframed Discover broadly (not "where you get involved"). Guideline added to `features/demo-mode.md` → "Card-copy guideline." → already in `features/demo-mode.md` (no further propagation needed)
- **FC18 group-shelter-walk demo slice built (WS-G) + `shelter` walkthrough re-authored around it (PO scope expansion).** New `GroupWalkSignupSheet` on the meet's `ShelterWalkPanel`: sign up to walk a shelter dog ON the trainer-led group walk → real meet-linked shelter-walk `Booking` (`dropoffMeetId`), two-tier (vouched direct / newcomer mentored + `beginMentorship`). The `shelter` walkthrough collapsed 6→5 beats centered on the group-walk sign-up as the appealing mentored first walk (was: see-the-walk + separate 1-on-1 mentor booking + solo walk). Faked/narrated (FC18 deferred): physical multi-dog checkout/release, mentor-as-responsible-party, group waivers. → `features/shelters.md` ("Group shelter walk (FC18)") + `strategy/Demo Narrative.md` (shelter arc → 5 beats) + `Future Considerations` FC18 (mark the slice shipped, deferred parts remain) + `Groups & Care Model.md` (group-walk as a config-#2 linked booking)
- **Mentor-as-discovery-bridge: the Beat 1→2 "how do they find the group walk" gap closed (PO direction, WS-G4).** The shelter posts no event CTA (anti-scoreboard), so the group walk had to be discoverable *through a person*. New `getMentorGroupWalks(mentorId, shelterId)` (`lib/mockMeets.ts`). `MentorListSheet` rows now carry a violet **"Runs group walks"** line for mentors who host one (Klára yes, Pavel no). `MentorSessionBookingSheet` **features the mentor's group walks at the top** (tap → `onClose` + route to `/meets/<id>`), with the 1-on-1 booking demoted beneath an "Or book a private 1-on-1 walk" divider — PO calls: group walk *featured* (paid the same, scales to multiple sign-ups), and *extend the existing sheet* rather than build a new one. Shelter walkthrough Beat 1 re-routed through this (now Walk Nora → Walk with a mentor → Klára → her group walk → the meet), replacing the Beat 2 `startUrl` teleport. **Verified end-to-end** (Eliška) + via the live walkthrough (Beat 1 = 4 steps, lands on the meet, Beat 2 picks up). → `features/shelters.md` (mentor list "Runs group walks" + booking-sheet featured group walks) + no other propagation (extends existing FC18 slice).
- **`MentorSessionBookingSheet` rebuilt into one unified two-step flow (PO direction 2026-06-23).** The featured-shortcut-that-bounces-to-the-meet was the wrong shape. The sheet is now: **Step 1 "Before your first walk"** — the shared credential frame (progress tracker, reworded so it explicitly governs *both* paths: "Each mentored walk counts, group or 1-on-1") + the layered waivers (gate both paths, so they "start it"); **Step 2 "Choose your walk"** — an explicit two-option choice between Klára's trainer-led **group walk** (free, the on-ramp; dog pre-set from the dog page, an optional "View the walk →" link, no meet-page bounce) and a **private 1-on-1** (paid, date/time). PO calls baked in: group sign-up **completes in-sheet** (link-out optional); both paths **feed the same vouch tracker** — `committedSessions` now counts mentored group-walk bookings (meet-linked shelter walks) alongside 1-on-1 `mentorSession` bookings; the 1-on-1 now **inherits the dog** when entered from a dog page. Extracted `lib/groupWalkBooking.ts` (`buildGroupWalkBookingInput`) as the single booking-shape source for both this sheet and the meet-page `GroupWalkSignupSheet` (no drift). Shelter walkthrough **Beat 1→2 re-authored** to sign up in-sheet (Beat 1 = find+reach mentor, 3 steps; Beat 2 = waivers → explainer → probe → choose+sign-up, all on Nora's page; the open sheet survives the beat handoff — verified live). **Verified end-to-end** (both paths + the live walkthrough). → `features/shelters.md` (mentored-walk sheet: two-step, two-path, shared tracker) + `Groups & Care Model.md` (mentored group walk counts toward the vouch).
- **Mentored-walk sheet now self-narrates as a two-step wizard (PO note 2026-06-23).** A modal *can* be a guided multi-step flow — the walkthrough already proved it (separate cards for the waivers step and the choose-walk step render above the open sheet). To make the two steps obvious *in the modal itself* (not only via the walkthrough), added a `StepBreadcrumb` at the top — "Waivers › Choose your walk", active step highlighted — so a user on the waivers step sees a choose-walk step follows. No numbers, to stay clear of the dotted vouch tracker. → no feature-doc update needed (UI clarity on an already-documented surface).
- **Booking card + detail now name the mentor and the group walk (PO note 2026-06-23).** A mentored group-walk booking is a three-party thing — shelter owns Nora, Eliška walks, **Klára** mentors and is paid — but only owner (shelter) + carer (walker) live in the booking, so the host was invisible. Derived both from `dropoffMeetId` (→ meet → its creator is the host): the card gains a "with Klára · Saturday morning group walk" line under the service; the detail Info tab gains two tappable rows — "Klára Horáčková · Your mentor" (→ profile) and the group walk (→ meet). `getMeetById` added; mentor name resolved full via `getUserById`. **Flag for later:** the booking model itself still doesn't carry the mentor as a first-class party (we derive it) — fine for the demo, worth revisiting if mentored bookings get heavier. → `features/shelters.md` (booking surfaces name the mentor + linked group walk).
- **The mentored first walk is a PAID mentor session, not free (PO correction 2026-06-23).** The group option showed "Free" — but "free" describes the *meet*, not the *mentored session*. The choice is how Eliška's mentored walk runs, and Klára is paid her fee either way (the group format just lets several mentees pay at once — her incentive to run it). So a mentored (un-vouched) group walk now carries the host's per-session fee, **the same price as the 1-on-1**; only a vouched walker's plain volunteer walk is free. Threaded via `buildGroupWalkBookingInput`'s `mentorFee`; the meet-page sheet looks the fee up with `getMentorsForShelter`. `priceLabel`/`perSessionPrice` now gate "Volunteer · no charge" on `price.total === 0`, so the paid mentored walk reads "450 Kč / session" on the Volunteering tab + booking detail. → no feature-doc update beyond `features/shelters.md` (mentored walk is paid; vouched volunteer walk is free).
- **"Saturday morning group walk" now lands on a Saturday (continuity fix).** Its `daysFromNow(2)` seed drifted to whatever weekday the demo ran on (showed "Thu 25 Jun"), contradicting the title — visible now that the mentor sheet surfaces the walk's date. Added a `nextWeekday(weekday)` helper to `lib/mockDate.ts` and switched the meet to `nextWeekday(6)`. → no feature-doc update needed (mock-data continuity).
- **Nora's pre-seeded recap re-attributed Eliška → Jakub V. (continuity fix).** The `shelter` arc introduces Eliška as a newcomer who hasn't walked, but a 2-day-old recap authored by her sat on Nora's wall. Re-attributed to an established walker (`post-walker-jakub-nora`); Kateřina's adopter-interest comment rides with it (Beat 5 payoff intact). Eliška now only *reacts* to community posts (consistent with "following for a while") and authors her recap live in Beat 4. Beat 5 copy adjusted to "recaps of Nora, yours among them." → mock-data + Demo Narrative continuity; no feature-doc update needed.
- **Probe checkpoints shipped as a new `probe` interstitial mode (the interview mechanism).** Distinct chrome ("For the room" eyebrow + teal left accent bar) so they read as "pose this to the room." Five probes across the recruitment/validation paths (shelter ×2, trainer ×1; new-owner left probe-free). Source: the mentor-script `A#` set. → `features/demo-mode.md` (interstitial modes: add `probe`) + the probe copy is build-authored, ratify in review
