---
status: active
last-reviewed: 2026-06-12
review-trigger: "Update as items are walked, edit as scope adjusts"
---

# Adoption-Curious Journey — Walkthrough

Verification checklist for the Adoption-Curious Journey phase. **Concise by design** — surface what's worth your judgment, what risks regression, and what confirms the phase thesis (an adoption-curious newcomer walks shelter dogs and becomes an advocate; the advocacy loop is the adoption engine).

**How to use:**

1. Dev server on port 3000 (preview already running). Unlock password is the local-dev default.
2. Switch personas via the profile-name dropdown or `?as=<id>` (the new persona is `?as=eliska`).
3. Walk top-to-bottom — ordered "needs your eyeballs most" → "least."

**Build status (2026-06-12):** All workstreams shipped — A (persona + focal dog), B (doorway), C (group walk / FC18), D (advocacy loop), E (capstone), F (arc beats → Demo Narrative + this doc). Items are tagged `[A ✓]` / `[B ✓]` etc. by the workstream that built them. The checkboxes are yours to tick as you walk each one.

**New seed data this phase:**
- **Eliška Dvořáková** (`?as=eliska`) — the Adoption-Curious Explorer persona. Žižkov, no dog, walking shelter dogs to decide whether to adopt.
- **Nora** (`/dogs/shelter-dog-nora`) — the focal long-stayer at Útulek. Gentle, newcomer-walkable, 71 days in care.

---

## Open for your call

Decisions made during the build — ratify or redirect. Each has a path to see it in context.

- [x] **O1. Focal dog is a NEW seed (Nora) + relaxed over-applied handler gates `[A ✓]` (PO ratified 2026-06-12).** Two parts: (a) seeded **Nora** as the focal long-stayer — gentle, sociable, newcomer-walkable, overlooked-but-blooms-on-a-walk (the research's advocacy archetype); both adoption endings hang off her. (b) **Corrected over-gating** the PO flagged: `experiencedHandlersOnly` had been applied to calm senior large-breeds purely on size/breed (Šimon, Baron — both "calm, gets along with everyone"), contradicting the thesis that the mentor path is *how* a newcomer becomes competent. Removed it from both; the gate now sits on exactly one genuinely-hard dog (Berta — wary wolfdog mix). Also corrected my own framing: `soloOnly` blocks only *group* walks, not a vetted newcomer walking 1:1 — so Maja/Líza were never "unwalkable," just group-ineligible (reactive-on-leash justifies that). Net: a freshly-vetted newcomer now has real inventory; Nora is the *focal* dog among several walkable ones. (`/dogs/shelter-dog-nora`, `/dogs/shelter-dog-simon`)
- [x] **O2. Persona name + neighbourhood: Eliška Dvořáková, Žižkov `[A ✓]`.** Fresh name + a neighbourhood not yet used by a POV persona (geographic spread). Bless or rename. (`/profile?as=eliska`)
- [x] **O3. Eliška seeded as a Útulek Supporter + Follow button now derives from membership `[A ✓]`.** She starts a step in (already following), so a reviewer dropping into her story finds her embedded rather than cold. Side effect: the shelter Follow button now reads initial state from the supporters roster (she's the first real persona ever seeded as a supporter). Alternative: leave her unseeded and make "Follow" her first live beat. (`/shelters/utulek-liben?as=eliska` — button reads "Following"; Members tab → Supporters · 13)
- [x] **O4. Images generated + wired `[done 2026-06-12]`.** Placeholders replaced with real generated images: Eliška's portrait + Nora's shelter portrait (no nameplate), dedicated walk-recap photos (`post-nora-walk-eliska/marie.jpeg`) wired to the two recaps, and a group-walk cover (`group-walk-utulek.jpeg`). Prompt doc archived. (`/profile?as=eliska`, `/dogs/shelter-dog-nora`, `/meets/meet-utulek-group-walk`)
- [x] **O5. Meet↔shelter linkage for the group walk (DR4) `[C ✓]`.** Resolved: added a lightweight `Meet.shelterWalk?: { shelterId }` field (the Meet sources its dogs from the shelter roster), and the dog each vouched walker takes rides in the existing `MeetAttendee.dogNames` — no new attendee field. The Meet still belongs to a community group (Klára's, for discovery/green) while `shelterWalk` sources the dogs (Help a Dog/violet) — the green→violet funnel is the linkage. Alt considered: hanging the walk off a shelter-owned group (heavier).
- [x] **O6. Group dog-checkout depth + C did NOT peel `[C ✓]`.** Built unified (didn't peel). The dog-checkout is modeled at demo depth: vouched walkers are pre-paired with dogs in the roster (`dogNames`); the operational "shelter releases N dogs to a group at once, mentor as responsible party" logistics are NOT modeled (a coordinator/admin concern, FC16) — flagged for shelter interviews (FC18 tension #2). Reasonable to go deeper (per-walk dog-claim flow) if a shelter validates the format.
- [x] **O7. Adoption transition `[G ✓ — baseline built; take-over deferred, PO call 2026-06-12]`.** Resolved model: on adoption the dog becomes a **frozen "Adopted" profile** that persists on platform — works for any adopter (off-platform, or on-platform-fresh-start), no migration. **Built (G1–G3):** Adopted hero pill, suppressed Walk/Adopt actions, shed shelter-context chips, "Happy endings" on the shelter Dogs tab, excluded from Discover, corrected in-care counts; tagged walk recaps still resolve to her. **Deferred (option 1):** the *take-over* path (an on-platform adopter continuing the profile as their own *owned* dog) — it's the genuine cross-container migration (touches the owned-vs-shelter resolver + every `user.pets[]` read), not a clean fake, so it stays for a later phase with the real claim/consent flow. The celebration's "walkers keep her in their gallery" line is still copy-only. (`/dogs/shelter-dog-nora` → Adopt → Finalise → see the Adopted state across dog page / shelter Dogs tab / Discover)
- [ ] **O8. Advocacy framing vs commitment-pressure (D3) `[D ✓]`.** The long-stayer recaps lean on "she's looking for a home / spread the word" rather than "adopt her," and the doorway/walk copy repeats "no adoption obligation" — kept the encouragement on *sharing* (low-pressure) and the obligation language explicitly absent. Reviewer call on whether the tone lands. (`/dogs/shelter-dog-nora` Posts, `/shelters/utulek-liben` Feed)
- [x] **O9. Relaxed blanket `soloOnly`, reserved for genuine no-other-dogs cases `[A ✓]` (PO direction 2026-06-12).** Following the experiencedHandlersOnly fix (O1), the PO pushed the same logic onto `soloOnly`: the goal is socialisation (condition dogs young to be social, capable, happy), so solo-only shouldn't be a blanket default. Removed it from the reactive-on-leash dogs (Maja, Líza — reactive-on-leash is a management need the trainer-led group walk addresses) and the shy/gentle dog (Mila). It now sits on **one** genuine case: Berta ("needs a quiet home with no other dogs"). This reinforces a documented thesis (Product Vision "resolve, don't avoid"; Cold-Start Playbook "socialization denied by default") and is the strategic backbone of FC18's trainer-led group walk. **Logged as a research gap + product question** (does policy become a *trajectory* not a static flag?) in Open Questions for shelter interviews. Reviewer call: confirm the one-genuine-case posture, or name dogs that should stay solo. (`/dogs/shelter-dog-maja`, `/dogs/shelter-dog-berta`)

---

## Worth verifying

Behaviors that need a human at the keyboard.

- [x] **V1. Eliška renders coherently across her surfaces `[A ✓]`.** `?as=eliska` → `/profile` (locked, "No dogs added yet", no connections, Žižkov/Prague 3, May 2026), `/shelters/utulek-liben` (Follow reads "Following"), bottom-nav avatar. Should read as a newcomer a step in, nothing broken.
- [x] **V2. Eliška appears in the persona picker `[A ✓]`.** Profile-name dropdown and `/` Explore-freely both list "Eliška Dvořáková · Adoption-Curious Explorer." Picking her routes and persists.
- [x] **V3. Nora reads as the advocacy archetype `[A ✓]`.** `/dogs/shelter-dog-nora` — Long-stayer chip, gentle/sociable tags (no policy/eligibility chips, since she's newcomer-walkable), 71 days in care, the "overlooked, blooms on a walk" backstory, Walk + Adopt CTAs both present.
- [x] **V4. Eliška on the Members tab `[A ✓; self-listing added 2026-06-12]`.** `/shelters/utulek-liben?as=eliska&tab=members` → Supporters count is 13; the viewer's own row is **pinned to the top with a "(you)" marker** ("Eliška D. (you)"), the rest in recency order. (Self-listing pin+label mirror the meet People-tab convention; added as a side task. Her row still isn't a profile link — bridged-supporter linking is FC9 territory.) **Operator-action gate (2026-06-12):** per-walker tier controls (Promote/Demote/Credit walks/Remove) no longer leak to ordinary viewers — they render only under `?admin=1` (operator preview), which also shows an "Operator view (demo)" banner above the list. Without the flag (the default, incl. `?as=eliska`) the Members tab is read-only. This is the demo stand-in until FC16's real walker-pool management ships; gating it now keeps the persona view honest (a supporter shouldn't see staff controls).
- [ ] **V5. Doorway "explore before you commit" framing `[B ✓; reworked into a dynamic dismissible card 2026-06-13]`.** `/discover/help-a-dog?as=eliska` → between the Sort row and the dog grid, a calm **reassurance card** (HandHeart icon, light volunteer-violet wash — color-codes it to the volunteering domain and lets it stand out from the neutral page, matching the credential-pill / mentor-link violet): "Walk a shelter dog — no adoption obligation. Spend time with the dogs and see how it works. New here? Many people start on a group walk or alongside an experienced volunteer, then decide in their own time." The mentor/group-walk on-ramp is named as **framing, not linked** — this is a cross-shelter surface that can't honor a specific mentor booking; that path surfaces once the viewer picks a dog (PO call 2026-06-13). **Dynamic:** the card is *suppressed for active walkers* (anyone vouched at a shelter via static roster OR a vouched application — e.g. `?as=klara` shows no card; they're past the doorway) and **dismissible** for everyone else (X → persisted `doggo-helpadog-intro-dismissed`, clears on demo reset). Dogs-view only. (The group walk is discovered community-side — the host's group Meets tab + meets discovery — NOT from the shelter page: shelters don't list meets/mentors on their page, PO direction 2026-06-12. The earlier shelter-Feed callout was removed.)
- [ ] **V6. Mixed group walk — slim details panel + People-tab roster `[C ✓, reframed per PO]`.** `/meets/meet-utulek-group-walk` is a **mixed** community walk (some bring their own dog; vouched walkers pick up an Útulek dog on the way; newcomers come along). **Details tab:** a "Shelter dogs joining" summary card mirroring the "Who's coming" pattern — shelter-dog avatars (rounded squares) + "3 from Útulek Liběň" + "View all" → People tab, and (un-vouched viewers only) a quiet violet "🎓 Walk with a mentor ›" **eyebrow link** in the card footer (not a button). No walker→dog roster here. **People tab** carries the roster: own-dog attendees show their dog plainly (Magda/Žofka, Hana/Runa, Eva/Luna); picked-up shelter dogs are badged — card rows get a "🐾 Shelter dog from Útulek" line, private rows get "{dog} · shelter dog" (violet); dogless newcomers show "Coming along — no dog today". Viewer-aware: as `?as=klara` (vouched) the mentor CTA is hidden. Útulek permits group walks; Berta still solo (per-dog override wins).
- [ ] **V7. Advocacy loop end-to-end `[D ✓]`.** (a) Finish a shelter walk (e.g. as a vouched walker on a Nora booking) → lands on `/dogs/shelter-dog-nora?finished=1` with the "Great walk with Nora! 🎉 Share a moment" banner → "Share a moment" opens the composer **pre-tagged** Nora + Útulek; "Maybe later" dismisses (no pressure). (b) Recaps surface: `/shelters/utulek-liben` Feed + `/dogs/shelter-dog-nora` Posts show Eliška's + Marie's recaps. (c) Network reach: `/home?as=eliska` shows the recaps because she **supports** Útulek (new followed-shelter feed gate); the lead recap carries a network member's interested comment (Kateřina V.) — the advocacy→adoption-interest mechanism made visible. To shortcut to the banner without driving a full walk: `/dogs/shelter-dog-nora?finished=1`.
- [ ] **V8. Adoption capstone `[E ✓]`.** `/dogs/shelter-dog-nora` → "Adopt Nora" opens the inquiry sheet: no-obligation intro, the escalation ladder (walk → sleepover/foster → adopt) with "bringing her back is never a failure" framing, shelter-curated meet-and-greet (no auto-match). "Express interest" → "Interest sent". Demo shelter-side toggles drive the state machine: "Arrange meet-and-greet" → hero shows **Adoption pending**; "Finalise — network adopts" (primary, tied to Kateřina from the recap comment) or "— {viewer} adopts" (secondary) → **celebration** "{dog} found a home 🎉" + care stats suppressed. State persists + resets with demo reset; "Reset" clears. Full PetProfile→new-owner migration intentionally deferred (DR7).
- [ ] **V10. Adopted state propagates everywhere `[G ✓]`.** Drive `/dogs/shelter-dog-nora` → Adopt → "Arrange meet-and-greet" → "Finalise — network adopts" (or reuse a finalized Nora). Then confirm the **frozen Adopted profile** reads consistently: dog page shows a green **Adopted** pill, no Walk/Adopt actions, no Long-stayer chip (energy stays), no care stats; `/shelters/utulek-liben?tab=dogs` moves her into **"Happy endings 🎉"** (Adopted chip, no kennel stats) and out of the active grid; the Feed "dogs in care" count drops by one; `/discover/help-a-dog` no longer lists her. Her walk recaps still point at her (now Adopted) profile. Reset via the sheet's "Reset" or demo reset.
- [ ] **V9. Existing demo threads intact `[F ✓ — reviewer drive]`.** Smoke-checked (all key surfaces 200, TypeScript clean) but worth a real drive: V2 Daniel→Klára and Tomáš mentee arcs still work; no persona-switch regressions. (Note: the group walk was seeded into Klára's group `group-klara-training` and several supporting personas now attend it — confirm her group's Meets tab + the existing arcs still read right.)

---

## Decisions surfaced during walkthrough

Append as you walk. Each entry carries a `→ target-doc.md` for the close sweep.

- **Seeded Nora as a newcomer-walkable focal long-stayer, AND relaxed over-applied `experiencedHandlersOnly`.** The gate was on calm senior large-breeds (Šimon, Baron) purely on size/breed — contradicts the mentor-makes-newcomers-competent thesis. Removed from both; reserved now for genuinely hard dogs (Berta only). `soloOnly` (group-walk block, not a handler gate) left on reactive dogs. → `features/shelters.md` (per-dog policy seeding principle: `experiencedHandlersOnly` is rare, for genuinely difficult dogs; the mentor path is the competence ramp)
- **Shelter Follow button derives initial state from the supporters roster.** Previously hardcoded `false`; Eliška is the first real persona seeded as a supporter, so the button and the Members tab now agree. → `features/shelters.md` (action row) + `features/demo-mode.md` (hydration-gated identity state)
- **Adoption-Curious Explorer archetype is distinct from the Shelter Walker.** Walker walks *instead of* owning; Explorer walks *to decide whether* to own. → `strategy/User Archetypes.md` (shipped)
- **Relaxed blanket `soloOnly` to one genuine case (Berta); reactive-on-leash is a management need, not a solo lock.** Doggo's socialise-don't-avoid stance; trainer-led group walk (FC18) is the mechanism. Research gap + "policy as trajectory not static flag" question logged. → `planning/Open Questions` (logged) + `features/shelters.md` (per-dog policy seeding principle: gates rare + genuine)
- **(D) Shelter-walk finish routes to the dog page with a "Share a moment" prompt, not the booking summary.** The recap is the adoption ad, so the dog (not the paid-booking record) is the right next surface. Paid-care walks keep the sessions-tab summary. → `features/explore-and-care.md` (finish routing) + `features/shelters.md` (advocacy loop)
- **(D) Composer extended with `initialTags`; walk-finish opens it pre-tagged to dog + shelter.** Reusable beyond this flow. Composer still doesn't persist (FC13), so the recap that *surfaces* is pre-seeded — the established demo pattern. → `features/profiles.md` (post composer) / no behaviour change to persistence
- **(D) New home-feed gate: followed/supported shelters' posts surface in the supporter's feed.** Makes the Supporter relationship meaningful and is the advocacy network-reach mechanism (a recap reaches non-walker adopters who follow the shelter). Scoped to `ShelterProfile.supporters`; verified no regression to non-supporter feeds. → `features/shelters.md` + `strategy/Content Visibility Model.md` (feed sourcing gate)
- **(C/FC18) Group shelter walk = a Meet with `shelterWalk: { shelterId }`; dogs ride in `MeetAttendee.dogNames`.** Built unified (not peeled). Útulek `groupWalksPermitted: true` + vouchingNote rewritten to the socialise stance. **Reframed per PO (2026-06-12) to a MIXED community walk** — not shelter-exclusive: some attendees bring their own dog, vouched walkers pick up a shelter dog on the way, newcomers come along dogless. The roster moved off the (overloaded) details panel to the **People tab**, where a dog that resolves on the linked shelter's roster is badged as a shelter dog (PersonRow `contextLine` for card rows; a "· shelter dog" highlight added to `PrivateProfileRow` so the badge shows even on private/tier-3 rows — it's walk-level info, not private). Details panel is a "Shelter dogs joining" summary card (avatar pile + count + "View all" → People tab), mirroring the "Who's coming" card; the conversion is a quiet eyebrow link in its footer (see next entry). → `features/shelters.md` (group walk + policy) + `strategy/Groups & Care Model.md` (Meet variant — mixed walk) + FC18 (promote/retire at close)
- **(C polish) Mentor CTA on the shelter-walk card is an eyebrow LINK, not a button (PO direction 2026-06-12).** Two design-system conventions the PO stated, applied here + worth propagating in the later design audit: (1) **rounded-corner (non-pill) buttons should always be neutral `ButtonAction` variants** — coloured affordances read as pills or links, never rounded coloured buttons; (2) **no `text-brand` token** — the brand/colour tokens are applied directly as text/surface colour. The mentor CTA became a violet text+icon link (`.shelter-walk-mentor-link`). Violet stays inlined hex (`#6d28d9`) pending the `--volunteer-*` token family (FC11) — the audit should promote it + formalise the button-shape rule. → `implementation/design-system.md` (button-shape rule + volunteer-link pattern) + FC11 (violet token)
- **(B2 — reversed) Shelters do NOT list meets/mentors on their page (PO direction 2026-06-12).** The shelter-Feed "warm on-ramp" callout I'd added was **removed** — the shelter page stays browse-dogs / become-supporter / sign-up→pick-a-mentor. The group walk is discovered community-side (host's group, meets discovery). Connecting meets to shelter pages is a deliberate "maybe later." → `features/shelters.md` ("Group shelter walk (FC18)")
- **(Considered, no change) Kept the shelter-page "Following" button green, not violet.** Violet is reserved for volunteer-path actions only (the one-job rule), so tinting a community/supporter follow violet would dilute the signal and make "Following" inconsistent across surfaces (green on groups, violet on shelters). Green Follow (community) + violet "Walk a dog" (volunteer) is the colour system signalling two different axes. → no doc change; reinforces the violet=volunteer-only rule for the design audit
- **(C design) How a brought shelter dog connects: display-only now; intended = linked shelter-walk Booking via `dropoffMeetId`.** The group-walk roster badge is name-resolved display only — no Booking / walk-log / tier / report / advocacy (those run off the solo-walk Booking). Agreed intended model (not built): reuse the existing shelter-walk Booking + `Booking.dropoffMeetId` (the config #2 linkage), instructed-for-now; NOT an in-meet dog-picker (bypasses shelter sign-off). Checkout logistics → shelter interviews. → `features/shelters.md` ("Group shelter walk (FC18)") + `planning/Open Questions` (logged)
- **(G) Adoption transition: built the frozen "Adopted" profile baseline; deferred the take-over migration.** On adoption the dog persists as an on-platform Adopted profile (pill + suppressed actions + shed shelter-context + Happy-endings on the shelter + excluded from Discover + corrected counts), which works for any adopter with no migration. The take-over path (adopter continues it as their *owned* dog) is the real cross-container migration — touches the owned-vs-shelter resolver + every `user.pets[]` read — so it stays deferred to a later phase with the claim/consent flow (PO call 2026-06-12, option 1). → `features/shelters.md` (adoption transition) + `planning/Open Questions §1/§14` (resolved model + deferral)
- **(E) Adopt CTA replaced with a real funnel: interest → shelter-curated meet-and-greet → pending → adopted, as a state machine with non-failure off-ramps.** New `useAdoptionStore` (persisted dogId→stage, demo override) + `AdoptInquirySheet`. Network-adopt is the primary finalise (tied to the recap commenter Kateřina — advocacy loop closed); self-adopt secondary. Adopted → "Happy endings" celebration + care-stats suppressed. Full PetProfile→new-owner migration deferred (DR7). Shelter-side advances are demo state-toggles (FC16 is the real operator surface). → `features/shelters.md` (adoption funnel) + `features/explore-and-care.md` (if booking surfaces touched) + `planning/Open Questions §1/§14` (transition resolution; migration still open)
