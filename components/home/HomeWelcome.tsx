/**
 * @deprecated HomeWelcome was scrapped on 2026-04-25 during the Meets Deep Pass
 * walkthrough. Reasoning: the card was oversized, its "Find meets near you" CTA
 * duplicated the Discover page's purpose, and meet creation isn't a frequent-
 * enough action to justify a hero slot on the home feed.
 *
 * The component is no longer imported anywhere. File can't be deleted from the
 * sandbox; clean up at phase close alongside the other orphaned components
 * (`PostMeetReveal.tsx`).
 */

export {};
